# Orchestration Plan — 2026-04-20（#405, #411）

## 対象 Issue

| Issue | Title | 種別 | Priority |
|---|---|---|---|
| #405 | harness: 5-run aggregation to markdown report | FEATURE | 4 |
| #411 | harness: A/B comparison script for experiment evaluation | FEATURE | 10 |

## 種別分類

- **BUG_ISSUES**: なし（Phase 2.5 スキップ）
- **FEATURE_ISSUES**: #405, #411 → `/pm-auto-issue2dev`

## 依存関係

- **#404（analyze_run.py）merged 済み** → 両 issue の入力として利用
- #405: 新規 `scripts/report.py`（複数 run metrics → markdown summary、A/B compare mode 含む）
- #411: 新規 `scripts/compare.py`（2 run set → diff markdown、JSON option）
- **共通ファイル**: `.github/workflows/ci.yml` のみ（Python job へジョブ追加）
- **並列性**: **弱依存**（CI ジョブ追加の衝突可能性あり） — 並列可

## 並列実行グループ

- Group A: #405, #411 同時起動

## マージ推奨順序

1. #405 先（`--compare` mode が #411 の機能と部分被り、先行確認）
2. #411 後（CI 衝突時は rebase 指示）

## 各 Phase の扱い

| Phase | 扱い |
|---|---|
| 1 | plan-405-411.md（本文書） |
| 2 | 2 worktree 作成 |
| 2.5 | **スキップ** |
| 3 | 両 worker に `/pm-auto-issue2dev <N>` 並列送信、Sonnet 4.6 preset |
| 4 | 設計突合（`report.py --compare` と `compare.py` の責務分担、CI job の重複回避） |
| 5 | fmt/clippy/test + ruff + py_compile |
| 6 | #405 先 merge → #411 rebase（必要なら） → merge |
| 7 | **スキップ** |
| 8 | summary-405-411.md |

## Worktree 識別子

| Issue | Branch | Path |
|---|---|---|
| #405 | `feature/issue-405-report-py` | `../Anvil-feature-issue-405-report-py` |
| #411 | `feature/issue-411-compare-py` | `../Anvil-feature-issue-411-compare-py` |

## Acceptance 要件

### #405
- [ ] markdown 表として正常レンダリング
- [ ] analyze_run.py の JSON を入力に使う
- [ ] A/B 差分モード (`--compare A/ B/`) で差分 markdown

### #411
- [ ] 2 つの dir を食わせて 1 ページの markdown
- [ ] 重要 metric の改善率を色つき or 符号で示す
- [ ] JSON 出力オプション

## 設計突合観点

- `report.py --compare A B` と `compare.py A B` の責務境界
  - #405 本文: report.py の主機能は複数 run aggregation、`--compare` は副次的
  - #411 本文: compare.py の主機能は A/B diff に特化、改善率・JSON 出力
- 両 worker の設計書で重複範囲と差別化ポイントを確認、必要なら修正指示
