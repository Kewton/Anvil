# Orchestration Plan — 2026-04-20（#410, #404）

## 対象 Issue

| Issue | Title | 種別 | Priority |
|---|---|---|---|
| #410 | harness: model matrix execution in bench.sh | **FEATURE** | 9 |
| #404 | harness: analyze_run.py to compute standard metrics from a run dir | **FEATURE** | 3 |

## 種別分類

- **BUG_ISSUES**: （なし、Phase 2.5 スキップ）
- **FEATURE_ISSUES**: #410, #404 → `/pm-auto-issue2dev` で処理

## 依存関係

- #410: `scripts/bench.sh` の拡張（`--models` matrix 実行 + `matrix-report.md` 出力）。#403 merged 済み（f0e3abf）を前提。
- #404: 新規 `scripts/analyze_run.py` + `docs/metrics.md`。他 issue から独立。
- **共通ファイル**: 無し（#410 は `scripts/bench.sh` / `benchmarks/`、#404 は `scripts/analyze_run.py` / `docs/metrics.md`）
- **並列性**: **独立** — 完全並列実行可

## 並列実行グループ

- Group A: #410, #404 を同時起動

## マージ推奨順序

1. #404 先（他から参照されない独立スクリプト）
2. #410 後（bench.sh 拡張、matrix 実行レポートに analyze_run.py を使えば理想）

ただし両者は物理的にコンフリクトしないので、CI 通過順に merge で可。

## 各 Phase の扱い

| Phase | 扱い |
|---|---|
| 1 | plan-410-404.md（本文書） |
| 2 | 2 worktree 作成、CommandMate 登録 |
| 2.5 | **スキップ**（bug ラベルなし） |
| 3 | 両 worker に `/pm-auto-issue2dev <N>` を並列送信。Sonnet 4.6 preset で 1M context 回避 |
| 4 | 設計突合（衝突チェック：bench.sh 記述整合） |
| 5 | develop HEAD での fmt/clippy/test + shellcheck + python 構文 |
| 6 | 2 PR 作成 → CI → squash-merge |
| 7 | **スキップ**（`--full` 未指定） |
| 8 | `summary-410-404.md` 出力 |

## Worktree 識別子

| Issue | Branch | Path |
|---|---|---|
| #410 | `feature/issue-410-matrix-bench` | `../Anvil-feature-issue-410-matrix-bench` |
| #404 | `feature/issue-404-analyze-run` | `../Anvil-feature-issue-404-analyze-run` |

## Acceptance 要件

### #410
- [ ] `--models 'A,B,C'` で全 model 実行
- [ ] `matrix-report.md` で model 別指標表
- [ ] 途中中断で partial 結果が残る

### #404
- [ ] `scripts/analyze_run.py /path/to/run-dir > metrics.json` が stable な JSON を吐く
- [ ] 指標定義が `docs/metrics.md` に書かれている
- [ ] 過去 run dir でも同じ出力（backward compatible）
