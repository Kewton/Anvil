# UAT 024

## Date
- 2026-04-24

## Scenario
- UAT continuity run for `workspace/v0.1.0/uatspace/uatplan.md`
- Model: `qwen3.5:122b`
- Sandbox: `/Users/maenokota/share/work/localwork/codextest/sandbox_0424/uat_023`
- Session: `019dbca4-dfa9-7fe2-8c27-f401d8dcbab4`

## Observed Failure
- After scaffolding the Next.js app, the actor entered focused `Read/Edit` recovery on `app/page.tsx`.
- The model first re-read `app/page.tsx`, then attempted a giant full-file `Edit` that was truncated.
- The follow-up recovery turn still hung for minutes instead of producing a compact `Edit`.

## Root Cause
- Focused recovery still allowed another `Read` even after the target file had already been read successfully.
- The recovery note said "one compact Edit" but did not narrow the first repo edit into a concrete small slice, so `qwen3.5:122b` still tried to replace the whole scaffold with the full game implementation.
- After the truncated full-file attempt, the next recovery turn still had too much freedom about what block to change first, which led back into long non-streaming stalls.

## Fix Direction
- Once the focused recovery target has already been read, allow only `Edit` until the first successful repo edit lands.
- Add a stronger first-edit note for scaffolded `app/page.tsx` / `src/app/page.tsx` that limits the next change to a compact static game shell instead of full gameplay logic.
