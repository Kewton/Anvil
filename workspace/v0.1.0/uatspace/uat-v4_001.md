# UAT v4 run note

Date: 2026-04-26

## Scope

- Implemented the 1-7 countermeasures from the current issue summary.
- Added the v4 test plan at `workspace/v0.1.0/uatspace/v4/uat_scenarios_v4.md`.
- Ran Rust verification, deterministic generation matrix, representative build/test, and existing-code fallback checks.

## Implementation Summary

- Added abstract `FeatureProfile` classification for form, list, validation, calculation, visualization, persistence, accessibility, and test support.
- Narrowed fast deterministic polish fallback to safe quality-improvement wording so non-UI requests such as OAuth replacement do not get stolen by template fallback.
- Strengthened generated app templates with calculation, target decision, progress visualization, localStorage persistence, error feedback, and `aria-live` state output.
- Added `npm test` smoke scripts and `npm audit --audit-level=critical` visibility to framework app/game package generation.
- Added game smoke coverage so generated game packages validate `canvas`, `requestAnimationFrame`, input listeners, and Restart.
- Added the v4 quality layers L1-L5 and explicit regression scenarios.

## Verification

- `cargo fmt`
- `cargo test quality::tests`: 25 passed
- `cargo test`: 431 library tests plus integration/doc tests passed
- `cargo build --release`

## UAT Matrix

### Pass 1

- qwen3.5 Scenario 01 Next business app: passed, iter 1/50, 0s, deterministic app fallback.
- qwen3.5 Scenario 02 Nuxt app: passed, iter 1/50, 0s, deterministic app fallback.
- qwen3.5 Scenario 05 React game: passed, iter 1/50, 0s, deterministic app fallback.
- qwen3.6 Scenario 01 Next business app: passed, iter 1/50, 0s, deterministic app fallback.
- qwen3.6 Scenario 02 Nuxt app: passed, iter 1/50, 0s, deterministic app fallback.
- qwen3.6 Scenario 05 React game: passed, iter 1/50, 0s, deterministic app fallback.

### Representative build/test

Ran on qwen3.6 pass-1 outputs:

- Next Scenario 01: `npm install`, `npm run build`, `npm test` passed.
- Nuxt Scenario 02: `npm install`, `npm run build`, `npm test` passed.
- React game Scenario 05: `npm install`, `npm run build`, `npm test` passed.

Dependency note:

- `npm install` reported known moderate/high audit warnings for Next/Nuxt dependency trees. The generated packages expose `npm run audit` with a critical threshold; build and smoke tests passed.
- Initial npm install attempts failed inside the sandbox due DNS/network restrictions. Re-running with elevated network access succeeded.

### Existing-code checks

- Scenario 03 safe polish on existing Next app: passed, iter 1/50, 0s, `data-anvil-polish="v1"` added and `npm test` passed.
- Scenario 04 non-safe OAuth replacement request: passed, no deterministic polish fallback, no generic template replacement, 0 files edited within the bounded 2-iteration probe.

### Pass 2

- qwen3.5 Scenario 01/02/05: all passed, iter 1/50, 0s, `npm test` passed.
- qwen3.6 Scenario 01/02/05: all passed, iter 1/50, 0s, `npm test` passed.

## Remaining

- Full `npm install` and `npm run build` were performed on representative qwen3.6 outputs, not on every pass-2 output.
- Audit warnings remain visible by design; resolving transitive framework advisories would require dependency-version policy decisions beyond this fallback-quality change.
