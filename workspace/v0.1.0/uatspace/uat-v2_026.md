# UAT v2 026 Incident

## Summary

qwen3.5:122b / qwen3.5:9b の `uat-v2_026` で、3本目の Nuxt.js テトリス生成が停止した。

## Reproduction

- Workdir: `/Users/maenokota/share/work/localwork/codextest/sandbox_0424/uat-v2_026/instruction_3_tetris`
- Command: `anvildev -m qwen3.5:122b -y --sidecar-model qwen3.5:9b --auto-plan`
- Prompt: `あなたが考える最高に面白くかっこいいテトリスゲームを3011ポートで起動可能なNuxt.jsアプリとして開発してください。`

## Observed

- Nuxt project files were scaffolded.
- The agent edited `app/app.vue` to reference `~/components/TetrisGame.vue`.
- No `app/components/TetrisGame.vue` or `components/TetrisGame.vue` was created.
- After that edit, the loop stayed in `thinking... (qwen3.5:122b)` for more than 2 minutes and did not apply the deterministic quality fallback.

## Root Cause

`first_existing_impl_target` did not include Nuxt 3's `app/app.vue` path, so the quality gate inspected less relevant targets and missed the incomplete shell. Because the incomplete shell was not converted into the deterministic Vue fallback immediately after the edit, the model entered another long reasoning turn instead of finishing with a playable vertical slice.

## Fix Direction

- Include `app/app.vue` in Nuxt target discovery and nested project detection.
- Add regression coverage for incomplete Nuxt shell output that references an uncreated component.
