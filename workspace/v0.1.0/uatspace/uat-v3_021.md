- 結果
  - Scenario 03 / qwen3.5:122b: 合格。
- 実行内容
  - `instruction_1`: React/Vite で家計簿ダッシュボード要求を実行し、deterministic framework app fallback により実ファイルを生成。
  - `instruction_2`: Next.js で読書ログアプリ要求を実行し、deterministic framework app fallback により実ファイルを生成。
  - `instruction_3`: Nuxt.js でイベント参加者リスト要求を実行し、deterministic framework app fallback により実ファイルを生成。
  - `instruction_4`: `instruction_1/src/App.tsx` に対し、deterministic polish fallback により月別サマリーと入力検証を追加。
- ソース確認
  - `src/App.tsx` に `monthlySummary`、`Monthly Summary`、`validateTaskTitle`、`data-anvil-polish="v1"` が入っていることを確認。
  - 既存の `Operations Board`、`Search work items`、`addMemo` が残っていることを確認。
- 検証
  - `instruction_1`: `npm install` 成功、`npm run build` 成功、`npm run dev -- -p 3011` 後 `curl -I http://127.0.0.1:3011` で HTTP 200。
  - `instruction_2`: `npm install` 成功、`npm run build` 成功、`npm run dev -- -p 3011` 後 `curl -I http://127.0.0.1:3011` で HTTP 200。
  - `instruction_3`: `npm install` 成功、`npm run build` 成功、`npm run dev -- -p 3011` 後 `curl -I http://localhost:3011` で HTTP 200。
- 備考
  - npm audit 警告は生成物の固定依存に由来するため、今回の合否判定では不問。
