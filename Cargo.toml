[package]
name = "anvil"
version = "0.0.2"
edition = "2024"

[dependencies]
rustyline = "15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = { version = "1", default-features = false, features = ["std"] }

[dev-dependencies]
tempfile = "3"
