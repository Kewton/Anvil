# CLAUDE.md

## Repo Intent

このリポジトリは `workspace/v0.1.0` に基づく Rust 版 local-first coding agent の実装。
旧来の Anvil 拡張ではなく、vibe-local 的な小さい Ollama 専用実装へ振り切っている。

## Current Architecture

- `src/photon/*`
<<<<<<< HEAD
  - Issue #554 で導入。Photon サイドカーへの HTTP クライアント（`pub mod photon`、integration tests からアクセス可能）。`schema.rs`: `HealthResponse` / `ContextPackRequest` / `ContextPackResponse` / `EvaluateRequest` / `EvaluateResponse` の serde 型定義（`serde_json::Value` newtype で暫定スキーマ）。`client.rs`: `PhotonClient { base_url, http: reqwest::blocking::Client }` + `send_failopen<T: DeserializeOwned>` DRY ヘルパー（エラー時は `session::feedback::mask_secrets` + `tracing::warn!` + fail-open 返し）。`health() -> bool` (GET /health, fail-open: false) / `context_pack() -> Option<ContextPackResponse>` (POST /v1/context/pack) / `evaluate() -> Option<EvaluateResponse>` (POST /v1/evaluate)。`Config.photon_url: String` (常に検証済み文字列、`validate_localhost_url` でバリデーション、デフォルト `http://127.0.0.1:3030`) / `Config.photon_timeout_ms: u64` (デフォルト 200ms、`ANVIL_PHOTON_TIMEOUT_MS` env で上書き、有効範囲 1-60000ms)。`Agent.photon: Option<PhotonClient>` は `config.offline=true` または `config.photon_enabled=false` のとき `None`、初期化失敗は `tracing::warn!` + `None`。E2E テスト: `tests/photon_client_smoke.rs` に mockito::Server を用いた 14 ケース（Ollama 不要）、`tests/config_tests.rs` に Photon 設定テスト 8 件。`mapper.rs`: Issue #555 で導入。Anvil 状態から `ContextPackRequest` へ変換する純関数層。公開定数 7 種（`PHOTON_CONTEXT_PACK_SCHEMA_VERSION=1`、各フィールドのバイト上限・件数上限）。`PhotonGateInputs { photon_present, shadow_mode, canary, session_id, turn_idx }` + `should_send_context_pack(&PhotonGateInputs) -> bool`（shadow=true 常時 / canary=0 非送 / canary=1000 常時 / 中間は `deterministic_canary_hash`(SHA-256 下位 8 bytes) permille サンプリング）。`ContextPackInputs<'a>` + `RecentToolCall { name, args_summary }`（tool output 非包含）+ `build_context_pack_request(&ContextPackInputs) -> ContextPackRequest`（フィールド別 `mask_secrets` + truncate → `serde_json::json!` 組み立て → `mask_payload_inplace` 最終防衛線 → `ContextPackRequest(value)`)。セキュリティ: touched_files の `..` component drop / absolute path drop / `mask_secrets` / `mask_payload_inplace` 全体適用。`RecentToolCall.name` は `mask_secrets` + 64 bytes cap + `[A-Za-z0-9_-]` 以外 `_` 置換 + 空なら `"unknown"` fallback。`layer 規約 (DR3-002)`: agent 層型を import しない。E2E テスト: `tests/photon_mapper_smoke.rs` に 20 ケース（Ollama 不要）。`prompt.rs`: Issue #557 で導入。`ContextPackResponse` を `"Photon Context:\n- …"` プロンプトセクション文字列へ変換する安全な純関数レンダラー。公開定数 4 種 (`MAX_PROMPT_ITEMS=5` / `MAX_PROMPT_SCAN_ITEMS=50` / `MAX_PROMPT_ITEM_CHARS=200` / `MAX_PROMPT_TOTAL_CHARS=800`)。`render_context_pack(&ContextPackResponse) -> Option<String>` が唯一の公開 API（I/O 無し・unsafe 無し・agent 層 import 無し）。パイプライン: `parse_items`（items[] 投影: kind/summary/source のみ、`MAX_PROMPT_SCAN_ITEMS` 上限で DoS 防御）→ `is_summary_kind` フィルタ → `mask_item`（`mask_payload_inplace` でオブジェクト全体マスク）→ `extract_summary_text`（`mask_secrets` 適用）→ `normalize_summary_text`（ASCII control / bidi control 文字を空白へ、連続空白を collapse: CR/LF/TAB/\u{200b}-\u{feff} 対象）→ `contains_prompt_injection`（`PROMPT_INJECTION_PATTERNS` 18 種 case-insensitive、`[inst]` / `system:` / `developer:` / `tool_call` / boundary escape 等）→ `contains_destructive_command`（`DESTRUCTIVE_PATTERNS` 7 種）→ `take(MAX_PROMPT_ITEMS)` → `truncate_chars(MAX_PROMPT_ITEM_CHARS)` → `build_section`（total cap `MAX_PROMPT_TOTAL_CHARS`）。`pub(crate)` helper 群は module unit tests U1-U6 (16 関数) で直接検証（DR3-001）。`invoke_photon_context_pack` (turn.rs) は shadow_mode gate → canary gate（`should_send_context_pack` SSOT）を HTTP fetch より前に評価し（DR3-002）、fetch 結果を `render_context_pack` に通してから `photon_context_pack_response` にセットする。E2E テスト: `tests/photon_prompt_smoke.rs` P1-P14 (17 ケース、Ollama 不要) / `tests/photon_turn_hook_smoke.rs` T9-T10 追加（canary=0 / shadow_mode=true で HTTP 0 回を検証）。Issue #583 で warnings ベースの seed filter を追加。公開定数 5 種を追加 (`MAX_PHOTON_WARNINGS_SCAN=64` / `MAX_BLOCKED_SUMMARY_IDS=32` / `MAX_BLOCKED_SUMMARY_ID_BYTES=256` / `MAX_PHOTON_WARNING_MESSAGE_BYTES=2048` / `BLOCKED_WARNING_REASON="premature_termination_risk"`) + 公開型 `RenderStats` 8 field（`items_blocked` / `items_adopted` / `items_accepted_pre_total_cap` / `items_filtered_kind` / `items_filtered_missing_text` / `items_filtered_security` / `items_over_cap` / `items_dropped_total_chars`、保存則: `items_in_response = sum of 7 disjoint buckets`）。`render_context_pack` signature を `(resp, blocked_ids: &HashSet<String>) -> (Option<String>, RenderStats)` に変更（Option A、純関数性維持）。`pub(crate)` 内部 API として `extract_blocked_summary_ids(&ContextPackResponse) -> (HashSet<String>, BlockedIdsStats)` / `sanitize_summary_id(&str) -> Option<String>` / private `parse_warning_message(&str) -> Option<(&str, &str)>` を追加（DR3-001: 内部 API は `src/photon/mod.rs` から re-export しない）。`sanitize_summary_id` は SSOT で `mask_secrets(raw) != raw` / secret-like word（token/secret/password/api_key/access_key/client_secret 部分一致）/ ASCII allowlist `[A-Za-z0-9._-]` 違反 / ASCII control + Unicode bidi 文字を `_` 置換 / 空・byte cap 超過を fail-open で drop（DR4-002）し、HashSet 挿入側（extract）と比較側（`project_item_fields` の `id` field）の双方で同じ正規化を通す。`project_item_fields` の allowlist に `"id"` を追加し、id field のみ `sanitize_summary_id` を通す（非文字列・rejected は drop）。warning filter は photon サイドカーの `warnings[]` に `kind="summary_quality_gate"` + `message="<summary_id>: premature_termination_risk[: <detail>]"` 形式の警告がある item を render pipeline から skip する。`build_section_with_stats(&[String]) -> (Option<String>, adopted: usize, dropped: usize)` 純関数を追加し、`MAX_PROMPT_TOTAL_CHARS` cap 適用後の実出力 item 数を `RenderStats.items_adopted` に反映（DR3-002: post-turn evaluate の `items_adopted_count` と prompt 実注入数が乖離しないことを保証）。Module unit tests U7 (12+ ケース) + WF-01〜WF-08 + SEC-01〜SEC-03 を直接検証。`invoke_photon_context_pack` (turn.rs) は HTTP fetch 成功直後に `config.photon_respect_warnings` ゲート → `extract_blocked_summary_ids` → blocked>0 のとき `agent.photon_context_pack.warning_blocked` event emit (7 keys: `session_id` / `turn_index` / `blocked_summary_ids` / `total_warnings` / `total_blocked` / `truncated_scan` / `truncated_unique`) → `render_context_pack(&resp, &blocked_ids)` → `completed` event に `warning_filter_enabled` / `items_blocked` 2 keys 追加 emit（常時、render_stats / config から populate、DR4-004 audit）。E2E テスト: `tests/photon_warning_filter_smoke.rs` WF-09〜WF-13 + SEC-04/SEC-05 (6 ケース、mockito、Ollama 不要、unique session_id filter で global OnceLock logger 衝突回避 DR3-003)。Issue #589 で photon の softer block (`admission_reason: "...next_hints suppressed: ..."`) を尊重する 2 段パイプライン化を追加。公開定数 2 種追加 (`MAX_PHOTON_ADMISSION_REASON_BYTES=512` / `PHOTON_HANDLED_MARKERS=["next_hints suppressed"]`、ASCII lower-case 固定 SSOT) + `BlockedIdsStats` に 2 フィールド追加 (`respected_by_admission_reason: usize` / `still_blocked: usize`、`#[derive(Default)]` 維持で 14 callsite 後方互換)。`pub(crate) fn is_already_handled_by_photon(reason: &str) -> bool` 純関数を追加（fail-closed 5 層防御: byte cap → ASCII control → bidi/format control (`is_bidi_control` 再利用) → `mask_secrets(reason) != reason` → `contains_secret_word(reason)` → ASCII-only case-insensitive `contains` match、Unicode 正規化 / homoglyph 展開なし）。`pub(crate) fn extract_photon_handled_ids(resp: &ContextPackResponse) -> HashSet<String>` を追加（`context_pack.items` → legacy `items` fallback、`MAX_PROMPT_SCAN_ITEMS=50` cap、`is_summary_kind(item)` ゲート (CB-001: kind=log 等の非 summary item による block bypass 防御)、`sanitize_summary_id` SSOT 適用）。`extract_blocked_summary_ids` は内部で 2 段パイプライン化（O(W+I) 保証）: warnings から block_set 構築 → `extract_photon_handled_ids` 呼び出し → `block_set.retain(|id| !handled_set.contains(id))` で差分 → `respected_by_admission_reason = before - after` / `still_blocked = blocked.len()` populate。戻り値型は `(HashSet<String>, BlockedIdsStats)` 不変。`turn.rs::invoke_photon_context_pack` の `warning_blocked` event を payload 7 → 9 keys 拡張（`respected_by_admission_reason` / `still_blocked` 追加、`is_secret_like_key=false`）、発火条件を `!blocked_ids.is_empty() || respected_by_admission_reason > 0` に拡張（全件 respected 時も監査用に emit）。`completed` event は不変（既存 11 keys）。`Config.photon_respect_warnings=false` 時は `extract_photon_handled_ids` も呼ばない（既存 WF-09 挙動と一貫、`BlockedIdsStats::default()`）。E2E テスト: `tests/photon_admission_reason_smoke.rs` AR-01〜AR-07 + AR-08 (CB-001 regression、計 8 ケース、Ollama 不要、unique session_id)。Issue #591 で photon `/v1/evaluate` adoption signal を補完。公開定数 `MAX_PHOTON_EVAL_ADOPTED_IDS=32` を追加（`MAX_BLOCKED_SUMMARY_IDS=32` 隣接 SSOT、`src/photon/mod.rs` re-export 経由で agent / session 層が参照、cap は agent 層 `invoke_photon_evaluate` が payload 組み立て直前で適用）。`RenderStats` を 9 → 10 field 拡張（新規 `adopted_summary_ids: Vec<String>`、post-total-cap で実際に emit された item の `sanitize_summary_id` 適用済 id 列、stable dedupe、id なし / sanitizer reject の item は `items_adopted` カウントに含めるが id 配列には含めない、S7-001）。`pub(crate) struct RenderCandidate { text: String, summary_id: Option<String> }` 内部値を新設（`src/photon/mod.rs` re-export なし、AS-09 / 設計判断 #6）し `build_section_with_stats` の入力型を `&[String]` → `&[RenderCandidate]` に変更、戻り値も `(Option<String>, adopted: usize, dropped: usize, adopted_ids: Vec<String>)` に拡張。`build_section` test wrapper は案 α（`&[String]` → `RenderCandidate { text, summary_id: None }` 詰め替えて delegate）で signature 維持。E2E テスト: `tests/photon_prompt_smoke.rs` SI-01〜SI-03 (3 ケース) / `tests/photon_evaluate_signal_smoke.rs` PES-01〜PES-05 + VR-07 / VR-09 / VR-10 (8 ケース、Ollama 不要、mockito `match_request` で `/v1/evaluate` body capture、unique session_id で log filter)。`eval.rs`: Issue #558 で導入。`EvaluateResponse` を `PhotonEvalSummary` に変換する純関数層。`pub fn parse_evaluate_response(resp: &EvaluateResponse) -> PhotonEvalSummary`（`admission_decision` 抽出: `"admission_decision"` 優先→ `"admitted"` bool フォールバック、`warnings` 件数上限 `MAX_PHOTON_EVAL_WARNINGS=8` + 文字数上限 `MAX_PHOTON_EVAL_WARNING_BYTES=512` + `mask_secrets`、`retry_summary` は `EvaluateResponse["retry_summary"]` 直接取り出し、モジュールローカル `truncate_field()` / `truncate_bytes()` helper）。layer 規約 (DR3-002): `PhotonEvalSummary` は session 層 `eval_log.rs` に定義（photon→session 方向が合法、session→photon は禁止）。E2E テスト: `tests/photon_eval_log_smoke.rs` T1-T6 (Ollama 不要)。`schema.rs`: Issue #559 で導入。serde roundtrip テスト: `tests/photon_schema_smoke.rs` S1-S10 (10 ケース、Ollama 不要)。`HealthResponse` ok=true/false / JSON表現 / `ContextPackRequest` / `ContextPackResponse` / `EvaluateRequest` / `EvaluateResponse` newtype serde roundtrip を `assert_newtype_roundtrip<T>(constructed: T)` DRY ヘルパーで検証。`provenance.rs`: Issue #594 で導入。`ContextPackResponse.context_pack.items[i].provenance` を抽出して `/photon-why` slash command と `agent.photon_context_pack.completed` event payload に surface する純関数層。公開定数 7 種（`MAX_PROVENANCE_OBJECT_BYTES=4096` / `MAX_PROVENANCE_SOURCE_ID_BYTES=256` / `MAX_CORRELATED_CASE_IDS=8` / `MAX_PROVENANCE_VERSION_BYTES=64` / `MAX_PROVENANCE_CREATED_AT_BYTES=64` / `ALLOWED_PROVENANCE_SOURCE_VALUES` 6値 (`human_handcrafted` / `anvil_case_record` / `user_slash_command` / `imported_from_repo` / `generated_by_llm` / `unknown`) / `ALLOWED_TRUST_TIER_VALUES` 3値 (`human_reviewed` / `auto_extracted` / `experimental`)）。公開構造体: `SeedProvenanceSummary` (Serialize-only、10 field: `summary_id` / `source` / `source_id` / `source_repo` / `trust_tier` / `anvil_version` / `photon_version` / `created_at` / `correlated_case_ids` / `provenance_status`、`source` は `&'static str` で fail-open placeholder `"unknown"` を取る、`provenance_status` 5値: `present` / `missing` / `invalid` / `oversized` / `sanitized`) / `ProvenanceSanitizeOutcome { value: Option<Value>, status: &'static str }`。公開関数: `extract_seed_provenance(summary_id, raw_provenance) -> SeedProvenanceSummary`（常に row を返す、PV-01 invariant 維持） / `sanitize_provenance_value(Option<&Value>) -> ProvenanceSanitizeOutcome`（5層 fail-closed: byte cap → ASCII control → bidi/format control (`prompt::is_bidi_control` SSOT、`pub(crate)` 昇格) → `mask_secrets(s) != s` → `prompt::contains_secret_word` SSOT 昇格 / 最終防衛線 `mask_payload_inplace`）。`prompt.rs` に `pub struct AdmittedItemView { summary_id / render_text / provenance }` + `pub fn enumerate_admitted_items_with_provenance(resp, blocked_ids) -> (Vec<AdmittedItemView>, RenderStats)` を追加し、`render_context_pack` も内部でこの SSOT を呼ぶ薄い wrapper に変更（signature 不変、production caller 影響ゼロ）。**Invariant (PV-01 / `debug_assert_eq!` で固定)**: `views.len() == stats.items_adopted` が常に成立する。`Agent` struct に 2 field 追加 (`last_injected_seed_provenance: Vec<SeedProvenanceSummary>` / `last_photon_context_pack_status: PhotonContextPackStatus`、`PhotonContextPackStatus` enum 7-variant: `NoTurn` (default) / `ShadowMode` / `CanarySkipped` / `PlanMode` / `Failed` / `NoInjection` / `Injected`)。`handle_user_message` 冒頭で `last_injected_seed_provenance.clear()` するが `last_photon_context_pack_status` は **意図的に reset しない**（`/plan` mode change 後も直前 Act turn lineage を保持、S7-002）。`agent.photon_context_pack.completed` event payload を 10 → 11 keys 拡張（`injected_seed_provenance_summary` を追加、各 item は 4 key の最小 JSON: `summary_id` / `source` / `trust_tier` / `provenance_status`、`is_secret_like_key=false` を PV-00 unit test で固定）。`/photon-why` slash command を `slash_commands::SLASH_COMMANDS` に追加 (`12 → 13 commands`) + `commands.rs::handle_command` に dispatch arm 追加し、`pub fn build_photon_why_message(status, &[SeedProvenanceSummary]) -> String` 純関数で 7 status variant の表示文言を SSOT 化（最終出力に `mask_secrets` を二重防御で適用、DR4-001）。E2E テスト: `tests/photon_provenance_smoke.rs` PV-01〜PV-17 (18 ケース、Ollama 不要、< 1 秒) + `tests/photon_schema_smoke.rs` S11 新規 + `src/photon/provenance.rs` `#[cfg(test)] mod tests` PV-00 (secret-like 命名検証) + 15 unit ケース + `src/photon/prompt.rs` invariant 5 unit ケース。layer 規約 (DR3-002): provenance.rs は agent / session 層型を import しない（`mask_secrets` は既存 prompt.rs 流儀踏襲）。
=======
  - Issue #554 で導入。Photon サイドカーへの HTTP クライアント（`pub mod photon`、integration tests からアクセス可能）。`schema.rs`: `HealthResponse` / `ContextPackRequest` / `ContextPackResponse` / `EvaluateRequest` / `EvaluateResponse` の serde 型定義（`serde_json::Value` newtype で暫定スキーマ）。`client.rs`: `PhotonClient { base_url, http: reqwest::blocking::Client }` + `send_failopen<T: DeserializeOwned>` DRY ヘルパー（エラー時は `session::feedback::mask_secrets` + `tracing::warn!` + fail-open 返し）。`health() -> bool` (GET /health, fail-open: false) / `context_pack() -> Option<ContextPackResponse>` (POST /v1/context/pack) / `evaluate() -> Option<EvaluateResponse>` (POST /v1/evaluate)。`Config.photon_url: String` (常に検証済み文字列、`validate_localhost_url` でバリデーション、デフォルト `http://127.0.0.1:3030`) / `Config.photon_timeout_ms: u64` (デフォルト 200ms、`ANVIL_PHOTON_TIMEOUT_MS` env で上書き、有効範囲 1-60000ms)。`Agent.photon: Option<PhotonClient>` は `config.offline=true` または `config.photon_enabled=false` のとき `None`、初期化失敗は `tracing::warn!` + `None`。E2E テスト: `tests/photon_client_smoke.rs` に mockito::Server を用いた 14 ケース（Ollama 不要）、`tests/config_tests.rs` に Photon 設定テスト 8 件。`mapper.rs`: Issue #555 で導入。Anvil 状態から `ContextPackRequest` へ変換する純関数層。公開定数 7 種（`PHOTON_CONTEXT_PACK_SCHEMA_VERSION=1`、各フィールドのバイト上限・件数上限）。`PhotonGateInputs { photon_present, shadow_mode, canary, session_id, turn_idx }` + `should_send_context_pack(&PhotonGateInputs) -> bool`（shadow=true 常時 / canary=0 非送 / canary=1000 常時 / 中間は `deterministic_canary_hash`(SHA-256 下位 8 bytes) permille サンプリング）。`ContextPackInputs<'a>` + `RecentToolCall { name, args_summary }`（tool output 非包含）+ `build_context_pack_request(&ContextPackInputs) -> ContextPackRequest`（フィールド別 `mask_secrets` + truncate → `serde_json::json!` 組み立て → `mask_payload_inplace` 最終防衛線 → `ContextPackRequest(value)`)。セキュリティ: touched_files の `..` component drop / absolute path drop / `mask_secrets` / `mask_payload_inplace` 全体適用。`RecentToolCall.name` は `mask_secrets` + 64 bytes cap + `[A-Za-z0-9_-]` 以外 `_` 置換 + 空なら `"unknown"` fallback。`layer 規約 (DR3-002)`: agent 層型を import しない。E2E テスト: `tests/photon_mapper_smoke.rs` に 20 ケース（Ollama 不要）。`prompt.rs`: Issue #557 で導入。`ContextPackResponse` を `"Photon Context:\n- …"` プロンプトセクション文字列へ変換する安全な純関数レンダラー。公開定数 4 種 (`MAX_PROMPT_ITEMS=5` / `MAX_PROMPT_SCAN_ITEMS=50` / `MAX_PROMPT_ITEM_CHARS=200` / `MAX_PROMPT_TOTAL_CHARS=800`)。`render_context_pack(&ContextPackResponse) -> Option<String>` が唯一の公開 API（I/O 無し・unsafe 無し・agent 層 import 無し）。パイプライン: `parse_items`（items[] 投影: kind/summary/source のみ、`MAX_PROMPT_SCAN_ITEMS` 上限で DoS 防御）→ `is_summary_kind` フィルタ → `mask_item`（`mask_payload_inplace` でオブジェクト全体マスク）→ `extract_summary_text`（`mask_secrets` 適用）→ `normalize_summary_text`（ASCII control / bidi control 文字を空白へ、連続空白を collapse: CR/LF/TAB/\u{200b}-\u{feff} 対象）→ `contains_prompt_injection`（`PROMPT_INJECTION_PATTERNS` 18 種 case-insensitive、`[inst]` / `system:` / `developer:` / `tool_call` / boundary escape 等）→ `contains_destructive_command`（`DESTRUCTIVE_PATTERNS` 7 種）→ `take(MAX_PROMPT_ITEMS)` → `truncate_chars(MAX_PROMPT_ITEM_CHARS)` → `build_section`（total cap `MAX_PROMPT_TOTAL_CHARS`）。`pub(crate)` helper 群は module unit tests U1-U6 (16 関数) で直接検証（DR3-001）。`invoke_photon_context_pack` (turn.rs) は shadow_mode gate → canary gate（`should_send_context_pack` SSOT）を HTTP fetch より前に評価し（DR3-002）、fetch 結果を `render_context_pack` に通してから `photon_context_pack_response` にセットする。E2E テスト: `tests/photon_prompt_smoke.rs` P1-P14 (17 ケース、Ollama 不要) / `tests/photon_turn_hook_smoke.rs` T9-T10 追加（canary=0 / shadow_mode=true で HTTP 0 回を検証）。Issue #583 で warnings ベースの seed filter を追加。公開定数 5 種を追加 (`MAX_PHOTON_WARNINGS_SCAN=64` / `MAX_BLOCKED_SUMMARY_IDS=32` / `MAX_BLOCKED_SUMMARY_ID_BYTES=256` / `MAX_PHOTON_WARNING_MESSAGE_BYTES=2048` / `BLOCKED_WARNING_REASON="premature_termination_risk"`) + 公開型 `RenderStats` 8 field（`items_blocked` / `items_adopted` / `items_accepted_pre_total_cap` / `items_filtered_kind` / `items_filtered_missing_text` / `items_filtered_security` / `items_over_cap` / `items_dropped_total_chars`、保存則: `items_in_response = sum of 7 disjoint buckets`）。`render_context_pack` signature を `(resp, blocked_ids: &HashSet<String>) -> (Option<String>, RenderStats)` に変更（Option A、純関数性維持）。`pub(crate)` 内部 API として `extract_blocked_summary_ids(&ContextPackResponse) -> (HashSet<String>, BlockedIdsStats)` / `sanitize_summary_id(&str) -> Option<String>` / private `parse_warning_message(&str) -> Option<(&str, &str)>` を追加（DR3-001: 内部 API は `src/photon/mod.rs` から re-export しない）。`sanitize_summary_id` は SSOT で `mask_secrets(raw) != raw` / secret-like word（token/secret/password/api_key/access_key/client_secret 部分一致）/ ASCII allowlist `[A-Za-z0-9._-]` 違反 / ASCII control + Unicode bidi 文字を `_` 置換 / 空・byte cap 超過を fail-open で drop（DR4-002）し、HashSet 挿入側（extract）と比較側（`project_item_fields` の `id` field）の双方で同じ正規化を通す。`project_item_fields` の allowlist に `"id"` を追加し、id field のみ `sanitize_summary_id` を通す（非文字列・rejected は drop）。warning filter は photon サイドカーの `warnings[]` に `kind="summary_quality_gate"` + `message="<summary_id>: premature_termination_risk[: <detail>]"` 形式の警告がある item を render pipeline から skip する。`build_section_with_stats(&[String]) -> (Option<String>, adopted: usize, dropped: usize)` 純関数を追加し、`MAX_PROMPT_TOTAL_CHARS` cap 適用後の実出力 item 数を `RenderStats.items_adopted` に反映（DR3-002: post-turn evaluate の `items_adopted_count` と prompt 実注入数が乖離しないことを保証）。Module unit tests U7 (12+ ケース) + WF-01〜WF-08 + SEC-01〜SEC-03 を直接検証。`invoke_photon_context_pack` (turn.rs) は HTTP fetch 成功直後に `config.photon_respect_warnings` ゲート → `extract_blocked_summary_ids` → blocked>0 のとき `agent.photon_context_pack.warning_blocked` event emit (7 keys: `session_id` / `turn_index` / `blocked_summary_ids` / `total_warnings` / `total_blocked` / `truncated_scan` / `truncated_unique`) → `render_context_pack(&resp, &blocked_ids)` → `completed` event に `warning_filter_enabled` / `items_blocked` 2 keys 追加 emit（常時、render_stats / config から populate、DR4-004 audit）。E2E テスト: `tests/photon_warning_filter_smoke.rs` WF-09〜WF-13 + SEC-04/SEC-05 (6 ケース、mockito、Ollama 不要、unique session_id filter で global OnceLock logger 衝突回避 DR3-003)。Issue #589 で photon の softer block (`admission_reason: "...next_hints suppressed: ..."`) を尊重する 2 段パイプライン化を追加。公開定数 2 種追加 (`MAX_PHOTON_ADMISSION_REASON_BYTES=512` / `PHOTON_HANDLED_MARKERS=["next_hints suppressed"]`、ASCII lower-case 固定 SSOT) + `BlockedIdsStats` に 2 フィールド追加 (`respected_by_admission_reason: usize` / `still_blocked: usize`、`#[derive(Default)]` 維持で 14 callsite 後方互換)。`pub(crate) fn is_already_handled_by_photon(reason: &str) -> bool` 純関数を追加（fail-closed 5 層防御: byte cap → ASCII control → bidi/format control (`is_bidi_control` 再利用) → `mask_secrets(reason) != reason` → `contains_secret_word(reason)` → ASCII-only case-insensitive `contains` match、Unicode 正規化 / homoglyph 展開なし）。`pub(crate) fn extract_photon_handled_ids(resp: &ContextPackResponse) -> HashSet<String>` を追加（`context_pack.items` → legacy `items` fallback、`MAX_PROMPT_SCAN_ITEMS=50` cap、`is_summary_kind(item)` ゲート (CB-001: kind=log 等の非 summary item による block bypass 防御)、`sanitize_summary_id` SSOT 適用）。`extract_blocked_summary_ids` は内部で 2 段パイプライン化（O(W+I) 保証）: warnings から block_set 構築 → `extract_photon_handled_ids` 呼び出し → `block_set.retain(|id| !handled_set.contains(id))` で差分 → `respected_by_admission_reason = before - after` / `still_blocked = blocked.len()` populate。戻り値型は `(HashSet<String>, BlockedIdsStats)` 不変。`turn.rs::invoke_photon_context_pack` の `warning_blocked` event を payload 7 → 9 keys 拡張（`respected_by_admission_reason` / `still_blocked` 追加、`is_secret_like_key=false`）、発火条件を `!blocked_ids.is_empty() || respected_by_admission_reason > 0` に拡張（全件 respected 時も監査用に emit）。`completed` event は不変（既存 11 keys）。`Config.photon_respect_warnings=false` 時は `extract_photon_handled_ids` も呼ばない（既存 WF-09 挙動と一貫、`BlockedIdsStats::default()`）。E2E テスト: `tests/photon_admission_reason_smoke.rs` AR-01〜AR-07 + AR-08 (CB-001 regression、計 8 ケース、Ollama 不要、unique session_id)。`eval.rs`: Issue #558 で導入。`EvaluateResponse` を `PhotonEvalSummary` に変換する純関数層。`pub fn parse_evaluate_response(resp: &EvaluateResponse) -> PhotonEvalSummary`（`admission_decision` 抽出: `"admission_decision"` 優先→ `"admitted"` bool フォールバック、`warnings` 件数上限 `MAX_PHOTON_EVAL_WARNINGS=8` + 文字数上限 `MAX_PHOTON_EVAL_WARNING_BYTES=512` + `mask_secrets`、`retry_summary` は `EvaluateResponse["retry_summary"]` 直接取り出し、モジュールローカル `truncate_field()` / `truncate_bytes()` helper）。layer 規約 (DR3-002): `PhotonEvalSummary` は session 層 `eval_log.rs` に定義（photon→session 方向が合法、session→photon は禁止）。E2E テスト: `tests/photon_eval_log_smoke.rs` T1-T6 (Ollama 不要)。`schema.rs`: Issue #559 で導入。serde roundtrip テスト: `tests/photon_schema_smoke.rs` S1-S10 (10 ケース、Ollama 不要)。`HealthResponse` ok=true/false / JSON表現 / `ContextPackRequest` / `ContextPackResponse` / `EvaluateRequest` / `EvaluateResponse` newtype serde roundtrip を `assert_newtype_roundtrip<T>(constructed: T)` DRY ヘルパーで検証。Issue #592 で `prompt.rs` に `RenderStats.adopted_summary_ids: Vec<String>` を追加（`render_context_pack` 内で採用 item の `sanitize_summary_id` 結果を parallel vec で収集し、`build_section_with_stats` の adopted count に合わせて truncate、空 id placeholder は最終 filter で除去）。`pub(crate) validate_user_feedback_input(&str) -> Result<(), FeedbackInputRejection>` を `prompt.rs` に追加（layer 2-7 SSOT、`FeedbackInputRejection` 6 variant `AsciiControl/BidiControl/MaskDivergence/SecretWord/PromptInjection/DestructiveCommand` + `as_reason_str()`）。`mapper.rs` に `pub(crate) build_feedback_evaluate_request(&FeedbackEvaluateInputs<'_>) -> EvaluateRequest` を追加（`schema_version=PHOTON_EVALUATE_SCHEMA_VERSION` / `agent` / `feedback_event {feedback_event_id, session_id, turn_index, command, outcome, summary_ids, draft_id, source_context_pack_request_id}` の JSON 構造、`mask_secrets` + `mask_payload_inplace` 最終防衛線）。`seed_draft.rs`: Issue #592 で導入。`PhotonSeedDraft` (`draft_id` / `created_at` / `command` / `repo_fingerprint: Option<RepoFingerprint>` / `source_context_pack_request_id` / `originating_summary_id` / `user_text` / `feedback_event_id`) を `state_root/photon_seed_drafts/<draft_id>.json` に persist (0o600 perms + `OpenOptions::create_new(true)` + directory symlink reject + per-file symlink reject + LRU eviction `MAX_PHOTON_SEED_DRAFTS=256` + `MAX_PHOTON_SEED_DRAFT_BYTES=16384` cap)。`derive_draft_id(payload, now_ms) -> "draft_<24hex>"` (sha256) + `validate_draft_id` allowlist `draft_[a-z0-9_-]{16,64}` + `-` 先頭拒否 (`tmp_tests::validate_test_id` 流儀)。Unit テスト 12 件 (validate_draft_id valid/invalid/traversal/uppercase/dash-prefix/empty + persist happy/oversize/invalid-id/symlink-reject/evict-cap + iter skip invalid/oversize)。
>>>>>>> f51b9fa (feat(photon-feedback): add /photon-thumbs-up/down/correct/rule slash commands with local seed drafts (Issue #592))
- `src/config.rs`
  - CLI / env / `.anvil/config` のマージ。Issue #583 で `Config.photon_respect_warnings: bool`（デフォルト `true`、`ANVIL_PHOTON_RESPECT_WARNINGS` env / `photon_respect_warnings=false` config file key で disable 可、CLI flag なし）を 4 site 統合（Config struct + PartialConfig + load_env_config + load_config_file + merge_partial_configs + Config::load の `unwrap_or(true)` SSOT 補完）。`photon_url` は既存 `validate_localhost_url` の localhost / credential なし制約を維持。Issue #592 で `Config.photon_common_seed_enabled: bool`（デフォルト `false`、`ANVIL_PHOTON_COMMON_SEED` env / `photon_common_seed_enabled=true` config file key で enable、CLI flag なし）を `photon_respect_warnings` と同じ 7-site SSOT パターンで追加（`/photon-rule` の persist+evaluate を gate; `false` のとき dry_run）。
- `src/model_registry.rs`
  - 利用可能モデルとメモリ量から main / sidecar を選択
- `src/ollama/client.rs`
  - `/api/tags` `/api/chat` 呼び出し
- `src/ollama/xml_fallback.rs`
  - `<think>` 除去と XML tool call 回収
- `src/tools/*`
  - built-in tools
  - `bash.rs`: Issue #461 で sandbox policy を強化。`pub(crate) fn check_blocked_command(&str) -> Option<BlockReason>` が destructive 判定の **Single Source of Truth** で、内部に既存 `BLOCKED_SNIPPETS` contains scan + 新規 `BlockCategory::{DangerousVerb, KillSignalOne, DeviceRedirect, ForkBomb}` の per-category predicate (`match_dangerous_verb`/`matches_kill_signal_one`/`matches_device_redirect`/`matches_fork_bomb`) を持つ。`render_block_error` が `"blocked dangerous command fragment: <pattern> (category=...)"` の Err 文字列を一元生成し、`registry.rs::classify_bash_dispatch_err` の prefix match 互換を維持。`pub(crate) fn apply_unix_pgroup` (cfg(unix) で `setpgid(0,0)` の `pre_exec` を内包、cfg(not(unix)) で no-op) と `pub(crate) fn terminate_child` を pair で expose（#458 generated test 経路から再利用予定）。`pub(crate) fn enforce_offline_policy` も同様に昇格。
  - `registry.rs`: Issue #461 で `preflight_bash_command` を新設し、`ToolRegistry::execute` (Bash) / `execute_bash_with_outcome` の両方で `maybe_confirm` の **前** に `check_blocked_command` preflight を実行（policy 判定の SSOT を `bash.rs` に集約、`registry.rs` は薄い policy ゲート）。`execute_bash_with_outcome` は `command` 取り出しを mode/scope/approval より先に引き上げ、`MissingArgument` が `ModeOrScopeDenied` より先に返る挙動変更を許容（regression-pinned）。
- `src/agent/prompting.rs`
  - Issue #469 で graph-aware ranking を導入。`WEIGHT_*` (file_name=8 / path_token=5 / path_contains=3 / content_token=2 / content_contains=1 / symbol=4 / test_impl_pair=7 / suspected_file=10 / changed_file=4 / graph_neighbor=6) と `BONUS_PATH_CONTENT_COHERENCE=3` の 11 要素を `prompting.rs` に SSOT 集約 (型は `usize`、`MIN_RANKING_SCORE=6` 据え置き)。`pub(crate) struct RepoContextInputs<'a> { repo_graph, suspected_files, changed_files, session_id, model }` 入力束ね型 (Default 派生) で signature を 3 引数化 (ISP)。`rank_repo_candidates` は `score_lexical` / `score_graph` (純関数) + graph 第二パス (`build_neighbor_index` + `resolve_import_target_to_file`) の 3 段で score 計算 (SRP / DIP)。`Option<Arc<RepoGraph>>` の Some/None で fallback (Completed/CacheHit→graph 加算、Failed/Disabled→lexical のみ)、詳細 build state は `agent.repo_graph.*` event を SSOT 参照。`ANVIL_NO_GRAPH_RANKING` で graph 由来 score を 0 に。jsonl event 4-suffix `agent.repo_context.{completed,skipped,failed,disabled}` を emit、payload は `logging::mask_payload_inplace` で recursive mask (Issue #461 流儀)。`graph_neighbor` は `RepoGraph::imports_of` の `ImportRef.target` を `sanitize_import_target` (空 / 512 byte 超 / NUL / control / absolute / `..` reject) + 曖昧 match → `None` の resolver で candidate file に解決した場合のみ score 付与。caller (`turn.rs::Agent::repo_context_message`) の `RepoContextCache` は cache key に `(task, work_root, repo_graph_present, last_feedback_kind, suspected_files_fingerprint, touched_files_fingerprint)` を含み、`pub(super) fn fingerprint_paths(paths: &[String]) -> u64` (sorted relative path list の `DefaultHasher` hash) で stale hit を回避。
- `src/agent/loop_run/spinner.rs`
  - 推論 / ツール実行中のスピナー（stderr, 80ms, TTY / `NO_COLOR` / `ANVIL_NO_SPINNER` / UTF-8 分岐）
- `src/agent/loop_run/interrupt.rs`
  - ESC 割り込み monitor（stdin raw mode + daemon thread、`ANVIL_NO_INTERRUPT` / 非TTY / approve prompt で自動無効化）
- `src/agent/loop_run/footer.rs`
  - 固定フッター daemon (stdout, 200ms, DECSTBM 再適用、token / mode / log / yes 表示、current_cols broadcast for turn.rs progress line)
- `src/tui/markdown.rs`
  - assistant 応答の SGR-only markdown renderer（行バッファ式、`<think>` strip、`ANVIL_NO_MARKDOWN` / `NO_COLOR` / `is_terminal` で多段無効化、session storage は raw LLM text のまま保持）
- `src/modes/plan_act.rs`
  - Plan / Act の単純な状態。Issue #576 で WorkMode 二段階分類のファーストパス層（純関数）を集約。`classify_work_mode_json(&str) -> WorkModeClassification` がモデル応答 JSON を `<think>` strip + first JSON object 抽出 + 許容 mode 6 値（`AnswerOnly` / `PlanOnly` / `EditFiles` / `RunCommands` / `Investigate` / `Mixed`）で正規化し、`Unknown` / `auto` / 不正値は `WorkModeClassification::fallback()` に倒す。セカンドパス発火条件は `should_request_confirmation(&WorkModeClassification) -> bool`（`ambiguity || confidence < WORK_MODE_CONFIRM_CONFIDENCE_THRESHOLD (0.90)`）で、`first_pass_has_explicit_no_edit_signal(&WorkModeClassification) -> bool` は LLM による権限昇格を防ぐ explicit-no-edit guard（adapter 層が後段で確認用に再評価）。値オブジェクト `WorkModeConfirmation` と `WorkModeConfirmationSource` enum 4 variants（Serialize only、`SkippedFirstPassUnambiguous` / `SkippedDisabled` / `SkippedRequestFailed` / `Confirmed`）も本ファイルに定義し、session 層 / agent 層への単方向依存を維持する（DR3-001: `answer_only_mode_active()` は session state のみ参照に変更し、agent 層を見ない）。
- `src/agent/loop_run/work_mode_confirm.rs`
  - Issue #576 で導入。WorkMode 二段階分類の adapter 層 + セカンドパス LLM 呼び出し。`classify_with_confirmation(&Agent, &str) -> WorkModeConfirmation` が公開 wrapper で、ファーストパス（`classify_work_mode_json`）結果を `should_request_confirmation` で評価してから sidecar model に 1 回だけ同期呼び出し（`tools=None`、JSON-only、`<think>` strip + first JSON object 抽出、許容 mode 6 値、`Unknown` / `auto` / 不正値 → fail-open でファーストパス採用）してセカンドパス mode を確定する。`work_mode_confirm_called_this_turn` (`Agent` field) で per-turn cap = 1、reset は `process_line` 冒頭。caller は turn.rs (`run_turn`) と commands.rs (`maybe_auto_plan_prompt`) の両方が同一 wrapper を呼び、結果を `WorkModeConfirmation` 値オブジェクトで受け取る（DRY）。セキュリティ: prompt 入力は `mask_secrets` + `WORK_MODE_CONFIRM_PROMPT_INPUT_MAX_BYTES=4KiB` cap、応答は `WORK_MODE_CONFIRM_RESPONSE_MAX_BYTES=16KiB` cap + 256 byte reason cap + `mask_payload_inplace` 最終防衛線、`WORK_MODE_CONFIRM_TIMEOUT_SECS=10` の明示 timeout、`first_pass_has_explicit_no_edit_signal` の二重評価で LLM による権限昇格を防ぐ。env: `ANVIL_NO_MODE_CONFIRM` で disable。ログイベント: `agent.work_mode.{confirmed,skipped,fallback}` (11 keys: `session_id` / `turn_index` / `model` / `first_pass_mode` / `first_pass_confidence` / `ambiguity` / `alternative_gap` / `second_pass_mode` / `latency_ms` / `parse_status` / `reason` / `source`)。既存 `agent.work_mode.classified` は変更なし、`turn_index` を additive key として追加。E2E テスト: `tests/work_mode_confirm_smoke.rs` WM-01〜WM-14 (Ollama 不要)。Out of Scope: SkillRegistry 登録、interrupt monitor 連携、EvalRecord 拡張、Config env override（将来 Issue で追加）。
- `src/agent/loop_run/photon_user_feedback.rs`
  - Issue #592 で導入。Photon seed への user-explicit feedback adapter（`/photon-thumbs-up` / `/photon-thumbs-down` / `/photon-correct "<text>"` / `/photon-rule "<text>"` の 4 slash command）。公開 entry point 4 つ (`handle_thumbs_up` / `handle_thumbs_down` / `handle_correct` / `handle_rule`、`Result<String, String>` を返し commands.rs 側 dispatch で `AgentEvent::Continue(Some(msg))` に変換)。Outcome literal 4 種 (`OUTCOME_USER_POSITIVE` / `_NEGATIVE` / `_CORRECTION` / `_RULE` の `&str`、client-side enum は作らず photon 側 `OUTCOME_VALUES` allowlist と一致させる Deployment Ordering Constraint 厳守)。`photon_feedback_disabled<F>` closure DI helper (`ANVIL_NO_PHOTON_FEEDBACK` env)。per-turn cap `Agent.photon_user_feedback_called_this_turn` は thumbs 経路で消費 (reset は `process_line` 冒頭、Plan mode early-return より前で `work_mode_confirm_called_this_turn = false` の隣)。`/photon-thumbs-{up,down}` は `Agent.last_injected_summary_ids` (turn.rs `invoke_photon_context_pack` で `RenderStats.adopted_summary_ids` から populate、HTTP 失敗 / shadow / canary / Plan early-return では `.clear() + last_injected_summary_turn_index=None`) が non-empty のときだけ `/v1/evaluate` を呼び、消費後に空に戻す。stale-turn ガード: `current_turn_index - last_injected_summary_turn_index > 1` で skip。`/photon-correct` は 8 layer 防御 (1: `PHOTON_USER_FEEDBACK_INPUT_MAX_BYTES=8KiB` cap / 2-7: `prompt::validate_user_feedback_input` SSOT 経由 `FeedbackInputRejection` enum 6 variant `AsciiControl/BidiControl/MaskDivergence/SecretWord/PromptInjection/DestructiveCommand` / 8: `serde_json::to_vec_pretty` 経由 JSON escape via `seed_draft::persist` + `serde_json::Value` via `reqwest::json`) → approval (yes_mode or TTY、default-deny) → 失敗アクション要約抽出 (`extract_failed_action_summary` で session.messages を tail-32 walk、assistant + tool_calls 優先) → `anti_pattern::extract_or_increment` best-effort (`workspace_key` / `work_root` / `active_task` / `touched_files` / `feedback_kind=UnknownFailure` 等を渡す、`SkippedNoActiveTask` 等は無視) → `PhotonSeedDraft` 構築 → Plan mode で dry_run, else `seed_draft::persist` → `/v1/evaluate` (`outcome=user_correction`)。`/photon-rule` は同パイプラインだが `config.photon_common_seed_enabled=false` のとき dry_run、`true` のとき `capture_repo_fingerprint(workspace_key, work_root, &[])` で `RepoFingerprint` を生成して draft に埋め込む。`feedback_event_id` は `(session_id, turn_index, command, now_ms)` の sha256 (uuid 依存追加なし、48 bytes 程度の `fb_event_<32hex>`)。ログイベント 5 sufix (`agent.photon_feedback.{completed,skipped,failed,disabled,rejected}`) で 7 key payload (`session_id` / `turn_index` / `command` / `summary_ids` / `outcome` / `dry_run` / `reason`、`mask_payload_inplace` 最終防衛線)。`SLASH_COMMANDS` に 4 command 追加 (`/exit` の直前)、`slash_commands_ssot.rs::slash_commands_contains_expected_16` で固定。Agent struct に 3 field 追加 (`last_injected_summary_ids: Vec<String>` / `last_injected_summary_turn_index: Option<usize>` / `photon_user_feedback_called_this_turn: bool`、`Agent::new` でゼロ初期化、`handle_user_message` リセット **しない**: 次 turn の thumbs まで持ち越し)。テスト seam `set_last_injected_for_test(&mut Agent, Vec<String>)` を `loop_run.rs` に `pub fn` で公開（production 経路は使わない）。E2E テスト: `tests/photon_user_feedback_smoke.rs` PUF-01〜PUF-13 (13 ケース、Ollama 不要、< 1 秒、mockito で `/v1/evaluate` を駆動、unique session_id で OnceLock 衝突回避 DR3-003)。Unit テスト: `photon_user_feedback::tests` 6 ケース (env gate / arg parse / generate_feedback_event_id format / rejection_reason / outcome literal 一意性) + `photon::prompt` の `ufv_*` 9 ケース + `photon::mapper` の `feedback_evaluate_tests` 3 ケース。Out of Scope: photon サイドカー側 `OUTCOME_VALUES` allowlist 拡張 (4 outcome literal を photon が受理する前提のデプロイ順序制約)、`anvil photon-seed-review` CLI、`EvalRecord` への feedback event 永続化、SkillRegistry 登録、`ANVIL_NO_PHOTON_FEEDBACK` 以外の env override（将来 Issue で追加）。
- `src/agent/loop_run/feedback_kind_confirm.rs`
  - Issue #579 で導入。FeedbackKind 二段階分類の adapter 層 + セカンドパス LLM 呼び出し（`work_mode_confirm.rs` 流儀踏襲）。`Agent::classify_with_feedback_confirm(&FeedbackKind, &str) -> Option<FeedbackKind>` が公開 wrapper で、success.rs facade が `VerifierOutcome::AutoTestRan` 受領直後 (`record_feedback_if_unset` 直前) に呼ぶ。strong-match SSoT 純関数 `should_request_feedback_confirmation` は `MARKER_CARGO_TEST_FAILED`（`"test result: failed"`）と `parse_pytest_failed` の numbered summary の 2 つだけを strong とみなし、それ以外の TestFailure (assert のみ等) と全ての UnknownFailure を second-pass 対象とする（S5-001 反映: `lower.contains("failed")` / `MARKER_PYTEST_FAILED_SUMMARY.trim_start()` の単純 substring match は禁止）。kind allowlist は 5 値固定（CompileError / TypeError / LintFailure / TestFailure / UnknownFailure、`kind_from_allowlist` match が SSoT、`#[serde(other)]` 非依存）。`VerifierOutcome::AutoTestRan` に `auto_test_combined_output: String` 追加（DR1-001: verifier_skill が `combined_output_for_classify(&result)` を 1 回評価して frame.kind 計算と outcome 伝播の両方に同じ文字列を使う）。`feedback_kind_confirm_called_this_turn` (`Agent` field) で per-turn cap = 1、reset は `run_turn` 冒頭、`model.is_some()` 時のみ wrapper 側で消費（`SidecarUnavailable` 経路は cap 不消費で次 turn 再試行可）。セキュリティ: prompt 入力は `mask_secrets` + `FEEDBACK_KIND_CONFIRM_PROMPT_INPUT_MAX_BYTES=8KiB` cap + `serde_json::to_string` で JSON escape (CB-003 layer 9)、応答は `FEEDBACK_KIND_CONFIRM_RESPONSE_MAX_BYTES=16KiB` cap + 256 byte reason cap + `mask_payload_inplace`、`FEEDBACK_KIND_CONFIRM_TIMEOUT_SECS=5` の明示 timeout、fail-open で first-pass kind を維持。env: `ANVIL_NO_FEEDBACK_KIND_CONFIRM` で disable。ログイベント: `agent.feedback_kind.{confirmed,skipped,fallback}` (10 keys: `session_id` / `turn_index` / `model` / `first_pass_kind` / `second_pass_kind` / `combined_output_bytes` / `latency_ms` / `parse_status` / `reason` / `source`、`build_feedback_kind_confirm_log_payload` SSoT)。Source enum 4-variant（FirstPass / SecondPassConfirmed / SecondPassOverridden / SecondPassFallback、#576 流儀）、Skip Reason 4-variant（PerTurnCapConsumed / PlanMode / EnvDisabled / HighConfidence）、Fallback Reason 6-variant（SidecarUnavailable / Timeout / TransportError / ResponseTooLarge / Empty / Malformed）で意味的に直交。`FeedbackKind::as_str()` を `session/feedback.rs` に新設（全 15 variant 網羅、`#[serde(rename_all="snake_case")]` と一対一）。`auto_test.rs` の `MARKER_CARGO_TEST_FAILED` / `parse_pytest_failed` / `combined_output_for_classify` を `pub(super)` 昇格（DR2-008、SSoT を本モジュールから参照）。E2E テスト: `tests/feedback_kind_confirm_smoke.rs` FK-01〜FK-13 (Ollama 不要、< 5 秒)。Out of Scope: FeedbackKind enum 拡張 (EnvironmentError / NetworkError 等)、EvalRecord への first_pass / second_pass 差分の永続記録、Tester 経路 `classify_tester_outcome` への適用、SkillRegistry 登録（別 Issue）。
- `src/agent/loop_run/quality_confirm.rs`
  - Issue #580 で導入。Quality-gate 二段階確認の adapter 層 + セカンドパス LLM 呼び出し（`feedback_kind_confirm.rs` 流儀踏襲）。`Agent::implementation_quality_issue_with_confirm(&mut self, request: &str, content: &str) -> Option<String>` が公開 wrapper で、`accepted_repo_change_quality_issue` / `accepted_repo_change_polish_target` の 2 callsite が `&self → &mut self` 化のうえ呼ぶ。`quality.rs` 側に `quality_first_pass_observation(request, content) -> QualityFirstPassObservation` (SSoT) と `QualityFirstPassGate { ConfirmationEligible / EarlyFail { reason: QualityEarlyFailReason } }` / `QualityEarlyFailReason` 4-variant（UiMarkerSpam / Placeholder / StrictSemantic / LowFidelityGame）を追加し、既存 `implementation_quality_issue_for_request` は薄い wrapper に縮退（既存 30 unit tests + 9 callsite tests と完全互換、DR1-010）。判定純関数 `should_request_quality_confirmation(obs) -> bool` は `EarlyFail` / `all_zero` / `all_strong (counts ≥ QUALITY_CONFIRM_STRONG_THRESHOLD=3)` をすべて false、それ以外 true（Type-B 救済: state_hits=0 だが interaction/feedback ヒットあるケースも second-pass 対象、S5-001 / 设计判断 #3）。双方向 override: first-pass Some + LLM interactive=true → None（Type-B 救済 SecondPassOverridden）、first-pass None + LLM interactive=false → Some("second-pass: not interactive UI")（Type-A 救済 SecondPassOverridden）、一致時は SecondPassConfirmed。`quality_confirm_called_this_turn` (`Agent` field) で per-turn cap = 1 + `last_quality_confirm_result: Option<(u64, QualityConfirmation)>` で memoization cache（content_hash key = `DefaultHasher` of `(request, full_content)`、設計判断 #5 / #7: #576/#579 と異なり Quality gate は同一 turn 内で最大 5 callsite から到達し得るため cap 消費後でも同一 content なら cached 結果を返して一貫性を保つ）。reset は `run_actor_loop` 冒頭（`feedback_kind_confirm_called_this_turn` と同ブロック）。セキュリティ 9 層: `tools=None` JSON-only call / `<think>` strip + first JSON object / strict `SecondPassResponse { interactive: bool, reason: Option<String> }` with `#[serde(deny_unknown_fields)]`（unknown field / non-bool / tool-call shape 拒否）/ reason `mask_secrets` + `QUALITY_CONFIRM_REASON_MAX_BYTES=256` cap / prompt 入力は `mask_secrets` → UTF-8 safe cap (request 4KiB / code excerpt head+tail 8KiB) → `serde_json::to_string` (CB-003) の順厳守 / `mask_payload_inplace` で log payload 最終防衛線 / `QUALITY_CONFIRM_RESPONSE_MAX_BYTES=16KiB` cap / `QUALITY_CONFIRM_TIMEOUT_SECS=5` の明示 timeout + fail-open（first-pass 維持）/ untrusted data 防御文言を prompt template に明記。env: `ANVIL_NO_QUALITY_CONFIRM` で disable（`quality_confirm_disabled` closure DI helper）。ログイベント: `agent.quality_gate.{confirmed,skipped,fallback}` (11 keys: `session_id` / `turn_index` / `model` / `first_pass_interaction_hits` / `first_pass_state_hits` / `first_pass_feedback_hits` / `second_pass_interactive` / `latency_ms` / `parse_status` / `reason` / `source`、`build_quality_confirm_log_payload` SSoT、raw request / raw content / prompt excerpt / content_hash 入力元は payload に含めない)。Source enum 4-variant（FirstPass / SecondPassConfirmed / SecondPassOverridden / SecondPassFallback、#576 / #579 流儀）、Skip Reason 6-variant（PerTurnCapConsumed / PlanMode / EnvDisabled / EarlyFail / AllStrongCounts / AllZeroCounts、early fail は LLM での権限昇格を防ぐ deterministic verdict 保護）、Fallback Reason 6-variant（SidecarUnavailable / Timeout / TransportError / ResponseTooLarge / Empty / Malformed）で意味的に直交。host 経路の影響（DR1-004）: `accepted_repo_change_polish_target` は first-pass `issue==None` 時のみ起動するため、second-pass で `interactive=false` （Type-A 救済 Some override）→ polish 自体を skip（quality issue 検出と判断、偽陽性で静的コードが polish されるのを防止）。`accepted_repo_change_quality_issue` は first-pass `Some(issue)` 時に起動し、second-pass `interactive=true`（Type-B 救済 None override）で quality issue を抑止できる。turn.rs:8863 `deterministic_framework_app_files_needed` (free fn) は構造判定のため first-pass のみ（設計判断 #6）。E2E テスト: `tests/quality_confirm_smoke.rs` QC-01〜QC-19 + HT-01〜HT-02（22 ケース、Ollama 不要、< 1 秒）。Out of Scope: SkillRegistry 登録、EvalRecord への first_pass/second_pass 差分の永続記録、ANVIL_NO_QUALITY_CONFIRM 以外の env override（将来 Issue で追加）。
- `src/session/*`
  - セッション保存と compaction
  - `discovery.rs`: `iter_session_dirs` 共有イテレータ（UUID/symlink/size/parse 防御）
  - `sessions_cli.rs`: `anvil sessions list|show|clean` と `--resume <ID>` の path confinement / plan / I/O
  - `feedback.rs`: `FeedbackFrame` / `FeedbackKind`（Bash / auto_test / tool parser / unsafe block / no-progress / edit failure を `SessionSnapshot.last_feedback` として正規化、excerpt 8 KiB cap + secret mask + workspace 相対 PathBuf、sealed `from_draft` ＋ `build_feedback_frame`）
  - `precaution.rs`: `WorkingMemory.active_precautions` 用の `Precaution` 型群（serde snake_case、deterministic id、status/severity/source enum）。Issue #453 で `severity_order(Severity) -> u8` helper を追加（High=0, Medium=Unknown=1, Low=2、turn.rs の prompt selector 用 stable sort key）。
  - `store.rs`: `WorkingMemory` 本体と `format_for_prompt()` wrapper / `format_for_prompt_with_precautions(&[Precaution])` 新メソッド (Issue #453)。後者は呼び出し側 (`turn.rs::select_precautions_for_prompt`) が severity sort + 関連度フィルタ + token budget cap (`MAX_ACTIVE_PRECAUTIONS_PROMPT=8`, `MAX_ACTIVE_PRECAUTIONS_CHARS=1024`) を適用済みの slice を渡す前提で render する。renderer は `status == Active` を防御的に再 filter (S5-001)。前者は full active-only list を渡す薄い wrapper で、Reminder Sidecar 経路 / 既存 4 regression test の互換維持に使う。Issue #456 で `SessionSnapshot` に AnvilScore 関連 5 field 追加 (`last_anvil_score` lossy persist + `unsafe_blocks_this_turn` / `repo_edit_succeeded_this_turn` / `touched_files_at_turn_start` runtime + `consecutive_no_progress_turns` session-cumulative)、`load_or_new` で `MAX_SESSION_JSON_BYTES` size cap を read 前検査。
  - `anvil_score.rs`: Issue #456 で導入。`AnvilScore` 値オブジェクト (12 field / 5 lifecycle group) + `compute_anvil_score` 純関数 + `AnvilScoreInputs` / `AnvilTestSummary` view + `AnvilScoreSnapshot<'_> { PreviousTurn / CurrentTurn }` enum (serde 非対応、Reminder 内部流通) + `format_for_prompt(MAX_RENDERED_CHARS=512)` 単一情報源 renderer + `sanitize` / `deserialize_lossy_anvil_score` (RawValue 経由、`MAX_ANVIL_SCORE_RAW_BYTES=64KiB` cap)。turn.rs から呼び出され、`agent.anvil_score.computed` jsonl event は `session_id` / `turn_index` / `score` / `render_chars` / `compute_ms` を payload に含める（`turn_index` は Issue #473 で追加、dataset export の join key）。
  - `export.rs`: Issue #473 で新規追加。`llm-io.jsonl` から `agent.reminder.completed` と `agent.anvil_score.computed` を `(session_id, turn_index)` で join し、fine-tuning 用 training JSONL を出力する。`ExportConfig` / `ExportScope` / `ExportFilter` / `ExportOutput` / `ExportResult` 型。`anonymize_paths` (unix only、`OnceLock<Regex>`、work_root prefix→`<workdir>`、home dir→`~/`) → `mask_secrets` の順で scrub。`run_export` (production) / `run_export_with_io<FR,FW,FS>` (closure DI seam、E2E `tests/dataset_export_smoke.rs` が 12 ケースを Ollama 不要で検証)。`MAX_EXPORT_JSONL_BYTES=64MiB` 超 session は parse 前 skip + `skipped_oversize_session_count`。必須フィールド欠落 record は skip + `skipped_missing_required_count`。`--output FILE` は symlink 拒否 + Unix 0600 新規作成。agent 層 import 禁止 (DR3-002)。
  - `case_retrieval.rs`: Case retrieval (Issue #463 / Epic C)。`state_root/cases/<case_id>.json` 群 (Issue #462 で永続化済み) を母集団に、現 turn の task / repo / stack / feedback と類似する case を 6 要素 Jaccard 重み付き和（`W_TASK=0.30 / W_STACK=0.10 / W_REPO=0.20 / W_FILES=0.20 / W_KIND=0.10 / W_PRECAUTIONS=0.10`、合計 1.0、`debug_assert` で保証）で score し、閾値 `CASE_RETRIEVAL_SCORE_THRESHOLD=0.40` 以上を `MAX_SELECTED_CASES=3` 件まで選定して短い summary を main prompt に注入する純関数。`pub fn retrieve_relevant_cases(state_root, &CaseRetrievalInputs, dry_run) -> Result<RetrievalOutcome, String>` が公開 API、`pub fn retrieve_with_iter<F>(.., iter_provider)` は E2E R6 (`iter_failure` 注入) 用の closure DI seam。`pub fn format_for_prompt(&[SelectedCase]) -> Option<String>` は per-case `MAX_CASE_RENDERED_CHARS_PER_CASE=240` / total `MAX_CASE_RENDERED_CHARS_TOTAL=1024` cap の単一情報源 renderer で、`mask_secrets` を二重防御として通す。layer 規約 (DR3-002): session 層に閉じ、agent 層型は `&[String]` plain slice / `&[PrecautionSnapshot]` (DR1-002 narrow) 引数で受け取る。`anvil_score.rs:32` の `use crate::agent::orchestration::*` 例外は本ファイルでは踏襲しない。corrupt CaseRecord は `read_and_score_one` が file 単位 skip、`completed.skipped_corrupt_count` で集計 (S3-007、retrieval 全体は継続)。env gate は `case_record` 系と同 closure DI signature の `case_retrieval_disabled<F>` / `case_retrieval_dry_run<F>` (`Fn(&str) -> Result<String, std::env::VarError>`)。
  - `case_photon_bridge.rs`: Issue #593 で導入。Phase A (Manual CLI + dry-run + JSONL local 出力)。`anvil sessions photon-promote` の核心ロジックを置く session 層モジュール。CaseRecord (#462) を photon `ActionSummary` v0.2 互換 JSON に変換し、`state_root/photon-promote-log.jsonl` (0o600 / append-only / 1 MiB cap) に dedup 履歴を残す。photon サイドカーへの HTTP 投入は Phase B (本 Issue 外)。レイヤリング規約 (DR3-002): 本ファイルは `use crate::photon::*` を **絶対に書かない**。photon 互換 serde 型 (`ActionSummary` / `Fact` / `Avoid` / `Hint` / `Provenance`) は session 層内で自前定義する (`PhotonEvalSummary` (eval_log.rs:162) と同じ流儀)。9 型 (`ActionSummary` / `Fact` / `Avoid` / `Hint` / `Provenance` / `BridgeOutcome` / `PromotedEntry` / `SkippedEntry` / `SkipReason`) + log entry 型 `PromoteLogEntry`。`SkipReason` は 6 variant Copy + `#[serde(rename_all="snake_case")]` (`AlreadyPromoted` / `NotSuccessState` / `EmptyLanguageStack` / `EmptyTaskSignature` / `StaleCase` / `Oversize`、設計判断 DR1-005 で 7→6) + `as_str()` SSOT。`ActionSummary` は `#[serde(rename="_provenance", skip_serializing_if = "Option::is_none")]` で provenance を photon schema の `extra` policy に依存させず attach。SSOT 定数 6 種 (`MAX_ACTION_SUMMARY_BYTES=16 KiB` / `FRESHNESS_THRESHOLD_DAYS=30` / `MAX_PHOTON_PROMOTE_LOG_BYTES=1 MiB` / `ACTION_SUMMARY_SCHEMA_VERSION="action-memory.v0.2"` / `SUMMARY_ID_PREFIX="anvil-case-"` / `PROVENANCE_SOURCE="anvil_case_record"`) + private const slice `FAILURE_KINDS` (9 件、`build_avoid` の filter 用)。`SummarizeRequest { summary: ActionSummary }` (POST /v1/summarize body wrapper) は Phase A では HTTP 投入未実装で到達不能のため定義しない (DR1-004、Phase B で本モジュール内に追加予定)。純関数 5 種: `is_verifier_active(score)` SSOT (DR1-002) / `is_eligible_for_promotion(case)` (CaseRecord 単一引数、verifier-less fallback は `user_visible_artifact && unsafe_actions_blocked==0 && consecutive_no_progress_turns==0`、DR1-001 / 設計判断 #4.5) / `derive_confidence_prior(score, verifier_active)` graduated 3-tier (1.0 / 0.8 / 0.6、設計判断 #2) / `check_quality_gate(case, &HashSet, now_unix) -> Result<(), SkipReason>` (6 種フィルタ、size cap は post-serialize で caller 側評価) / `convert_case_to_action_summary(case, session_id, extracted_at) -> ActionSummary` (純関数、`mask_secrets` + `apply_defensive_mask` 2 段防御)。private helper: `build_repo_id(fingerprint)` 3 段 fallback (git_remote → workspace_key → langhash、`mask_secrets` 通過) / `build_facts` / `build_avoid` / `apply_defensive_mask` (`mask_payload_inplace` 経由)。I/O 関数 4 種: `read_promote_log(state_root) -> Result<HashSet<String>>` (symlink reject、1 MiB cap 超 warn + 継続) / `append_promote_log_entry(state_root, entry)` (0o600 cfg(unix)、symlink reject、初回作成時のみ chmod) / `rotate_promote_log_if_needed(state_root)` (Phase A stub、size 超過 warn のみ、Phase B で per-entry LRU eviction) / `write_jsonl_output(path, &[ActionSummary])` (0o600 cfg(unix)、`create_new(true)`、symlink + 既存ファイル reject、親 dir 存在チェック)。RFC3339 builder `format_rfc3339_utc(unix_secs)` (chrono 不採用、Howard Hinnant の civil_from_days を inline 実装、新規 dep ゼロ)。CLI: `src/cli.rs::SessionsAction::PhotonPromote {session, case_id, all, dry_run, yes, print_summary, output}` (7 番目 variant、既存 6 variant の直後)。`src/session/sessions_cli.rs::run_photon_promote(workspace_root, state_root, session, case_id, all, dry_run, yes, print_summary, output) -> Result<i32, String>` (dispatch arm 6→7 拡張)。`--session` / `--case-id` / `--all` は相互排他、`--case-id` は `validate_case_id` allowlist 経由、非 dry_run + `!yes` は fail-safe で dry_run へ降格 + stderr warning。`extracted_at` は `SystemTime::now()` から RFC3339 を 1 回だけ取得し全 case で共有 (DR1-007)。session_id は CaseRecord に保持しないため `--session` 引数があれば使用、無ければ `"unknown"` fallback。photon URL credential 検証は MVP では HTTP 投入しないため `Config.photon_url` load 時 `validate_localhost_url` に委ね、CB-08 では `validate_localhost_url` を直接呼んで credential reject を確認。E2E テスト: `tests/case_photon_bridge_smoke.rs` CB-01〜CB-12 の 12 ケース + sanity 2 ケース (`empty_state_root_returns_ok` / `output_flag_writes_jsonl_one_line_per_summary`、計 14 ケース、Ollama 不要、< 1 秒、DR2-005 で旧 CB-09 photon mockito 502 経路は削除、Phase B で `tests/photon_promote_http_smoke.rs` 等で別途追加)。module 内 `#[cfg(test)] mod tests` は 38 ケース (3+3+4+7+3+2+5+3+4+4=38、各純関数の境界値と I/O round-trip を直接検証)。Phase B 拡張予定: HTTP POST `/v1/summarize` (`PhotonClient::summarize()`)、`SummarizeRequest` 型追加、`rotate_promote_log_if_needed` の per-entry LRU eviction 実装、`ANVIL_PHOTON_AUTO_PROMOTE` 自動 batch、CaseRecord への session_id 永続化 (`--session` filter の有効化)。
  - `case_record.rs`: CaseRecord (Issue #462 / Epic C)。Act mode の post-loop hook (Reminder の直後 / `agent.milestone.turn_completed` の直前) で、`last_anvil_score` の success 条件 (検証あり: build_passed / tests_passed / user_visible_artifact / consecutive_no_progress_turns / NoVerifier フォールバック: repo_edit_succeeded_this_turn / unsafe_blocks_this_turn / consecutive_no_progress_turns) を満たす turn から短い手続き記憶を抽出し、`state_root/cases/<case_id>.json` に永続化する。`CaseRecord` (case_id / created_at / repo_fingerprint / task_signature / language_stack / initial_feedback / successful_precautions / changed_files_summary / verify_commands / outcome_score) と支援型 (`RepoFingerprint`, `PrecautionSnapshot`, `CaseRecordInputs<'a>`, `CaseFileEntry`, `PersistError`) を閉じ込め、`From<&Precaution> for PrecautionSnapshot` も同ファイル側 (orphan rule 範囲内) に置く。`MAX_CASE_RECORD_BYTES=16 KiB`, `MAX_CASE_RECORDS=256` (lazy LRU eviction、`persist` 内で `iter_case_files(state_root)` 経由)。`case_id` は `case_[a-z0-9_-]{16,64}` allowlist + `-` 先頭拒否 (`tmp_tests::validate_test_id` 流儀)。scrub は `feedback::mask_secrets` / `logging::mask_payload_inplace` の SSOT 再利用 (#461)。git_remote 取得は `Command::new("git")` を `-c core.sshCommand= -c credential.helper= -c core.pager=cat -c core.hooksPath=/dev/null -c gpg.program=/dev/null` + 全 `GIT_*` env 削除 + 1s timeout で hardening し、結果を `mask_secrets` → `sanitize_git_remote` (rfind('@') で IPv6 / 多重@ / scheme 無し ssh shorthand 対応) に通す。per-turn cap = 1 (`SessionSnapshot.case_record_extracted_this_turn` `#[serde(skip, default)]`、`run_turn` 冒頭で false reset)、Plan mode disable、`ANVIL_NO_CASE_RECORD` / `ANVIL_CASE_RECORD_DRY_RUN` env (closure DI helper `case_record_disabled` / `case_record_dry_run`)。Logging 4 event 固定 (`agent.case_record.{extracted,skipped,failed,disabled}`、LLM 呼び出し無し)。CBR 検索 / 類似度判定 / 再注入 / CLI / Reminder からの直接参照は本 Issue Out of Scope (Epic C 配下の別 Issue で後追い)。
  - `eval_log.rs`: Structured evaluation log (Issue #471)。Act mode の post-loop hook (`agent.milestone.turn_completed` の直前) で turn-level aggregate snapshot を `state_root/sessions/<id>/logs/eval.jsonl` に 1 record/turn で追記する。`EvalRecord` (schema_version / session_id / ts_ms / task / model / mode / tool_protocol / tool_calls / feedback_frame / active_precautions / anvil_score / changed_file_classes / verify_commands / case_retrieval_result / final_outcome) と支援型 (`ToolCallSummary` / `FeedbackFrameSummary` / `AnvilScoreSummary` / `EvalPrecautionSnapshot` / `ChangedFileClasses` / `CaseRetrievalSummary`)。logger は `OnceLock<Mutex<File>>` の append-only SSOT、permissions 0o600。`init_eval_log` は lib.rs で warn-only 初期化 (DR3-002)。`build_eval_record` は純関数 (DR1-001、`ts_ms` caller 渡し)。`write_eval_record` は global logger 経由、`write_eval_record_to(&EvalRecord, &Mutex<File>)` は test isolation 用 direct write variant。`From<&AnvilScore>` / `From<&Precaution>` で型安全変換 (DR1-002、`EvalPrecautionSnapshot` に `kind` なし DR2-001)。セキュリティパイプライン (DR4-001): `to_value → mask_payload_inplace → to_string → scrub_absolute_paths (ANVIL_EVAL_SCRUB_PATHS opt-in) → size check (MAX_EVAL_LOG_RECORD_BYTES=64KiB) → append`。`ChangedFileClasses.impl_files` は `#[serde(rename="impl")]` で JSON 上は `"impl"` キー。`LoopStats` に `changed_impl_count / changed_test_count / changed_setup_count` を追加し、`build_stats` の全 `changed_files` カウント (truncation 前、DR3-001) から populate。tool_call_summaries は `focused_edit_recovery` truncation より前に収集 (DR3-003)。`CaseRetrievalSummary` は `try_inject_case_retrieval_message` の Completed ブランチで `format_for_prompt` 消費前にキャプチャ (DR2-005、`Agent.last_case_retrieval_summary: Option<CaseRetrievalSummary>` per-turn キャッシュ)。定数 SSOT: `MAX_EVAL_TASK_BYTES=4096` / `MAX_EVAL_TOOL_ARG_BYTES=1024` / `MAX_EVAL_FEEDBACK_EXCERPT_BYTES=8192` / `MAX_EVAL_PRECAUTIONS=8` / `MAX_EVAL_VERIFY_CMD_BYTES=4096`。Issue #558 で追加: `PhotonEvalSummary` struct (`photon_request_id / context_pack_id / admission_decision / warnings / prompt_adopted / task_outcome / retry_summary`) を session 層に定義（DR3-002: photon→session 依存方向を維持）。`EvalRecord` に `photon_eval: Option<PhotonEvalSummary>` フィールド追加。定数 `MAX_PHOTON_EVAL_FIELD_BYTES=256` / `MAX_PHOTON_EVAL_WARNINGS=8` / `MAX_PHOTON_EVAL_WARNING_BYTES=512` を追加。`build_eval_record()` に `photon_eval: Option<PhotonEvalSummary>` 引数追加（`case_retrieval_result` の直後）。`Agent` struct に `last_context_pack_id: Option<String>` (shadow mode 問わず抽出、`handle_user_message` でリセット) と `last_photon_eval_summary: Option<PhotonEvalSummary>` (`invoke_photon_evaluate` でセット、`build_eval_record` で `.take()` 消費、`run_turn` 冒頭でリセット) を追加。E2E smoke tests 12 件 (Ollama-free, `tests/eval_log_smoke.rs` R1-R12) + photon eval 6 件 (`tests/photon_eval_log_smoke.rs` T1-T6)。Issue #591 で `PhotonEvalSummary` に `summary_ids_adopted_count: Option<usize>` フィールド追加（`#[serde(default, skip_serializing_if = "Option::is_none")]`、agent 層 `invoke_photon_evaluate` が cap=`MAX_PHOTON_EVAL_ADOPTED_IDS` 適用後の `summary_ids_adopted.len()` を `Some(n)` で populate、shadow mode は `Some(0)`、photon disabled / fail-open は `None`）。`parse_evaluate_response` constructor は本フィールドを `None` 固定で初期化（payload cap 適用は agent 層責務、DR3-002 photon→session 単方向依存維持）。E2E テスト 2 件追加: `tests/photon_eval_log_smoke.rs` T7 (Some(n) → 数値 serialize) / T8 (None → field 省略 `skip_serializing_if` 検証)。
  - `rollout_policy.rs`: Issue #561 で新規追加。shadow mode から canary mode への移行条件判定モジュール。`collect_rollout_stats_from_state(state_root) -> Result<RolloutStats, String>` が全セッションの `eval.jsonl` を走査して `photon_eval_turns` 等を集計し、`evaluate_rollout_conditions(&RolloutStats, RolloutPolicyConfig) -> RolloutStatus` が純粋判定を行う（I/O と判定の分離）。`RolloutEvalRecordProbe` モジュール内ローカル構造体（`photon_eval: Option<Value>`, `photon_canary: u16`）で `EvalRecord` の Deserialize を全体に波及させずに必要フィールドのみ読む（設計判断 #3）。`ConditionStatus { Ok, Ng(String), ManualRequired(String) }` / `ConditionResult { id, label, status, note }` / `RolloutStatus { conditions, automatic_checks_passed, manual_required, ready_for_canary, eval_turns_found }` / `RolloutStats { photon_eval_turns, skipped_corrupt_records, skipped_oversize_sessions }` / `RolloutPolicyConfig { min_eval_turns }`。条件1/3/4 は常に Ok（実装済み）、条件2 は `photon_eval_turns >= min_eval_turns` で Ok、条件5 は常に ManualRequired（canary vs 非 canary の成功率比較は外部分析）。セキュリティ: `ensure_regular_file_inside_root` が symlink 拒否 + 通常ファイル確認 + `canonicalize` で path escape 検出、`MAX_ROLLOUT_EVAL_BYTES=64MiB` 超 session をスキップ、corrupt JSON は record 単位スキップ + 集計継続、エラーメッセージに raw path を含めない。CLI: `anvil sessions photon-rollout-check`（`SessionsAction::PhotonRolloutCheck {}` + `sessions_cli::run_photon_rollout_check`）。`Config.photon_rollout_min_eval_turns: u32`（デフォルト100、`ANVIL_PHOTON_ROLLOUT_MIN_EVAL_TURNS` env で上書き）、`resolve_photon_rollout_min_eval_turns(workspace_root)` で軽量単独取得。`EvalRecord.photon_canary: u16`（`#[serde(default)]`、`build_eval_record()` で 0 固定 + `turn.rs` で `record.photon_canary = self.config.photon_canary` に上書き）。E2E テスト: `tests/photon_rollout_policy_smoke.rs` R1-R12（Ollama 不要）。
  - `tmp_tests.rs`: tmp-test の lifecycle (#458)。`state_root/sessions/<id>/tmp-tests/{files,metadata}/` 配下で管理。`TmpTest`(serde snake_case) / `TmpTestStatus` (Draft/Promoted/Discarded) / `TmpTestRunResult` (将来用 reserved)。`create_generated_test` / `discard_tmp_test` / `promote_tmp_test` / `list_tmp_tests`。エラー型は既存 `Result<_, String>` を踏襲 (`thiserror` 不可、設計判断 #8)。`derive_test_id` (sha256 + created_at) / `validate_test_id` (`tmp_[a-z0-9_-]{16,64}` allowlist) / `validate_tmp_test_relative_path` (NUL/`..`/abs/1024 byte 拒否)。`MAX_TMP_TEST_METADATA_BYTES=64KiB`, `MAX_TMP_TESTS_PER_SESSION=4096`。promote は `auto_approve=false && interactive_approval=false` で拒否、衝突時は `--force` 必須、symlink dst は force 時も拒否、`OpenOptions::create_new(true)` で TOCTOU 回避。auto_test 経路には載せない。session 単位 cleanup は既存 `run_clean` が吸収。流儀は `precaution.rs` 寄り (型定義 + 自由な construction): promote/discard で status を上書きする lifecycle のため、`feedback.rs` の sealed `build_feedback_frame` 流儀は採用しない。`ToolContext.tmp_tests_root: Option<PathBuf>` (registry.rs の 9 番目フィールド) と shared helper `resolve_tmp_tests_path` で Read/Write/Edit の `tmp-tests/<rel>` prefix を session-scoped root に routing し、Plan mode 判定より前で評価することで Plan 中の LLM が tmp-tests へ書き戻すユースケースを塞がない。CLI は `anvil sessions tmp-tests {promote,discard,list} --session <id>` で sessions 体系に集約 (新規 top-level なし)。Issue #459 で 10 番目フィールド `ToolContext.tester_active: bool` を追加し、`enforce_tmp_tests_only_when_active` で Tester 起動中の Edit/Write を `tmp-tests/` prefix 強制 (registry 層の path confinement、in-memory のみで session JSON には書き出さない)。
- `src/util/file_classify.rs`
  - Issue #456 で導入。`is_test_file` / `is_setup_file` / `is_implementation_file` の単一情報源。`agent::orchestration::verify_repo_progress` と `session::anvil_score` の双方から参照され、layer 逆転 (session → agent) を起こさず DRY を維持する (DR1-007)。
- `src/util/git_hardened.rs`
  - Issue #468 で導入。`pub(crate) fn run_git(work_root, args) -> Option<String>` の単一情報源。case_record の private `run_git` を物理移設し、`session::case_record` と `repo_graph::fingerprint` の双方が util を `use` する形にした (DR1-001)。`cfg(unix)` 限定 (Windows は常に None で fallback、DR2-007)。`-c core.sshCommand= -c credential.helper= -c core.pager=cat -c core.hooksPath=/dev/null -c gpg.program=/dev/null` + `GIT_*` env scrub + 1s timeout の hardening をここに集約。
- `src/repo_graph/*`
  - Issue #468 で導入。`RepoGraph` v1 の独立 layer (5 ファイル: `mod.rs` 公開境界 + builder、`parse.rs` 言語別 regex parser、`pair.rs` `likely_covers` ペアリング、`persist.rs` JSON I/O + LRU、`fingerprint.rs` git rev + path hash)。`agent` / `session` のいずれにも依存せず、`util::file_classify` (#456) / `util::git_hardened` (#468) のみを `use`。public 4 型 (`RepoGraph` / `BuildOutcome` / `BuildOptions` / `RepoGraphError`) + ImportRef/ImportKind に絞る (DR1-004)。const SSOT 5 種 (`MAX_REPO_GRAPH_FILES=50_000` / `_DEPTH=32` / `_FILE_BYTES=1MiB` / `_TOTAL_BYTES=16MiB` / `_FILES_PERSISTED=8`)。永続化先 `state_root/repo_graph/<fingerprint_id>.json` (16-hex sha256 of git_rev + work_root_canonical + state_root_canonical、DR1-007)。schema_version: u8 を JSON top-level に保持し corrupt は file 単位 skip + best-effort remove (DR1-008)。`Agent::new` 直後に同期 1 回 build (per-turn cap 不要)、event 4 サフィックス `agent.repo_graph.{completed, skipped, failed, disabled}` (built/cache_hit は payload reason で区別、DR2-004)。env gate `ANVIL_NO_REPO_GRAPH` で disable、`ANVIL_REPO_GRAPH_DRY_RUN` は closure DI helper `repo_graph_dry_run`。parser は regex のみ (`std::sync::OnceLock<Regex>` 採用、once_cell 不使用、新規 dep ゼロ、DR2-003)。SkillRegistry には乗せない (3-fence 不適合 / SkillTrustTier 不明確、DR2-008)。SessionSnapshot 非 embed (`turn.rs::run_turn` の per-turn cap reset 列に追加しない、DR1-006 / S3-006)。
- `src/logging.rs`
  - `init_logging` / `log_llm_event`。Issue #461 で `pub(crate) fn mask_payload_inplace(&mut Value)` と `pub(crate) fn is_secret_like_key(&str)` を新設し、`log_llm_event` の冒頭で payload tree を recursive mask する。`Value::String` leaf は `mask_secrets` (token / kv / URL) を通し、`Value::Object` の secret-like key (API_KEY/TOKEN/SECRET/PASSWORD/ACCESS_KEY/CLIENT_SECRET 部分一致 + `_KEY`/`_TOKEN` suffix) は value type に関係なく `Value::String("***")` で置換（DR4-003: secret-like key 配下は object/array/number/bool/null も string に変わる schema 例外。非 secret key 配下は型不変）。
- `src/agent/skills/mod.rs`
  - Issue #465 の `AgentSkill` trait + `SkillRegistry` 基盤。`SkillTrigger` 3-fence (`IterationInternal` / `MessageBuild` / `PostLoop`)、`SkillInput` / `SkillOutput` enum (#466 で `Verifier(Box<VerifierOutcome>)` variant 追加 + `#[non_exhaustive]` 付与、DR-466-004)、skill 名 allowlist `^[a-z][a-z0-9_]{2,32}$`、`catch_unwind` 不使用 (D4-001)。`skipped_event_key` / `failed_event_key` の `match` allowlist は `reminder` / `verifier` の 2 名 + fallback (#466 で `verifier` 追加)。release build の `debug_assert` allowlist は外部入力 security boundary ではなく、trusted in-process static registration 専用 (DR4-003)。
  - Issue #467 で `SkillTrustTier` enum 4 variant (`BuiltInReadOnly` / `BuiltInCanWriteTemp` / `BuiltInCanRequestBash` / `ExternalDisabled`、`#[non_exhaustive]` + `serde(rename_all="snake_case")`、`#[serde(other)]` 不使用 = 未知 variant は parse error)、`AgentSkill::tier(&self) -> SkillTrustTier` 必須メソッド (default なし、設計判断 #1)、`SkillOutput::PermissionDenied(SkillPermissionDenial)` 新 variant、`SkillInvocationResult::permission_denied(denial)` factory (`attempted_execute=false` で per-turn cap 不消費) を追加。`SkillRegistry::invoke()` の判定順は `lookup → pre_check → applicability → tier_check (新) → execute`、tier_check は **private** 純関数 4 つ (`evaluate_trust_tier` / `tier_capability_label` / `build_permission_denied_payload` / `skill_bypasses_plan_mode`) で構成。`agent.skill.permission_denied` event の payload key 6 値 (`session_id` / `model` / `skill_name` / `tier` / `requested_capability` / `reason`) は `is_secret_like_key=false` を満たすよう命名 (DR1-005 / DR2-006)。Plan mode bypass allowlist は `verifier` のみ (DR-466-001 互換、3 件目で trait method 昇格を再評価、設計判断 #4)。`ReminderSkill::tier() = BuiltInReadOnly` は本 Issue では `Agent::maybe_invoke_reminder` direct call 経路のため production 経路で実効稼働しない宣言値 (DR3-002)、registry 経由化は将来 Issue で追加。`VerifierSkill::tier() = BuiltInCanRequestBash` が production で実効稼働する唯一の skill。`SkillOutput::PermissionDenied` は `SkillRegistry::invoke` 内で生成・consume が完結する不変条件で、turn.rs facade の `match SkillOutput` には届かない (DR1-002 1 次防御)。facade は `_ => debug_assert!(false, "PermissionDenied must not reach facade")` を 2 次防御として持つ。Tool 層 `enforce_tmp_tests_only_when_active` (#459) との関係は **補完的防御** (threat scope 異なる、DR2-003)。
- `src/agent/skills/reminder_skill.rs`
  - Issue #465 の ReminderSkill adapter。`name() -> "reminder"` / payload は Issue #473 で 17 key に拡張（`turn_index` / `task_at_call_time` / `precautions_at_call_time` / `feedback_excerpt` / `added_precautions_text` 追加）。`reminder.rs:1207-1536` の 4 regression test を SSOT として保持。Issue #466 では `Agent::new` に register したが direct call 経路 (`Agent::maybe_invoke_reminder`) を残しており、registry 経由の差し替えは将来 Issue で追加。
- `src/agent/loop_run/verifier_skill.rs`
  - Issue #466 で導入。`VerifierSkill` (unit struct) は post-loop 単独 fence で `AutoTestRunner::detect/run` + `compute_anvil_score` を担当する `AgentSkill` trait 実装。`pre_check = None` / `applicability = true` (DR-466-001 / DR1-004): Reminder / CaseRecord / CaseRetrieval / AntiPattern の 4 skill とは異なり Plan mode disable しない (Plan でも AnvilScore 計算は維持)。`VerifierInputs` 7 field (`score_inputs` / `repo_verification` / `should_dispatch_success_verifier` / `protocol_demands_verifier` / `changed_files` / `tester_candidate_some` / `workspace_root`) を facade が組み立て、`VerifierOutcome` 5 variant (`AutoTestRan` / `AutoTestTransportError` / `TesterDelegated` / `NoVerifier` / `Skipped`) を返す。turn.rs facade が `[a]-[e]` の 5 step で side-effect 適用 (legacy `agent.autotest.completed` / `agent.anvil_score.computed` event emit / verify_commands push / `record_feedback_if_unset` (#455 first-eligible-failure-wins) / `try_invoke_tester` 委譲 / `exit_reason::TransportError` 反映)。`agent.verifier.completed` event の payload 4 key (`session_id` / `model` / `dispatched` / `compute_ms` ※`compute_ms` は facade で computeし event payload 直接書き込み) は新規。`AutoTestKindView` (Build/Test) は `auto_test::AutoTestKind` (`pub(super)`) の skill 層 view。`sanitize_verify_command_for_case_record` は CaseRecord 連携前の secret mask + control char flatten + 4096 byte cap (DR4-001)。Tester / CaseRecord / CaseRetrieval / AntiPattern の skill 化は本 Issue Out of Scope (Epic D 配下の別 Issue)。
- `src/agent/loop_run/reminder.rs`
  - Reminder Sidecar (#452): FeedbackFrame の失敗系 kind が記録された後、sidecar Ollama を 1 回だけ同期呼び出し（`tools=None`、JSON-only、`<think>` strip + first JSON object 抽出、tool_calls 非空は no-op）して `WorkingMemory::add_precaution` に書く。closure-based pure 関数 `run_reminder_with_strategy` + `Agent::maybe_invoke_reminder` wrapper（iteration-internal hook + post-loop hook）。`reminder_called_this_turn` で per-turn cap = 1。`ANVIL_NO_REMINDER` で disable。Logging: `agent.reminder.{completed,failed,skipped}` を `llm-io.jsonl` に。sidecar の `active_precautions_summary` 入力は `format_for_prompt()` wrapper 経由で full active-only list（cap 適用前）を受け取り、重複 push 抑止精度を保つ (Issue #453 設計判断 #5)。Issue #456 で `ReminderInputs.anvil_score: Option<AnvilScoreSnapshot<'_>>` 追加。`AnvilScore::MAX_RENDERED_CHARS = 512` で renderer 側 truncate、prompt boundary は `# Active precautions` の後 / `# Latest feedback` の前で、最終 `# Output\nJSON only.` guard が残ることを test で固定。iteration-internal hook では `PreviousTurn(&last_anvil_score)`、post-loop hook では `CurrentTurn(&computed_score)` を `anvil_score_computed_this_turn` フラグで切り替える (DR1-006)。Issue #473 で `ReminderInputs.active_precautions_at_call_time: &'a [String]` 追加・`build_log_payload` を `(outcome, session_id, model, turn_index: usize, inputs: Option<&ReminderInputs<'_>>)` に拡張。`agent.reminder.completed` payload が 17 key に拡張（`turn_index` / `task_at_call_time` / `precautions_at_call_time` / `feedback_excerpt` (4KiB cap + mask_secrets) / `added_precautions_text` 追加）。`REMINDER_LOG_FEEDBACK_EXCERPT_BYTES=4096`。
- `src/agent/loop_run/tester.rs`
  - Tester Skill v1 (#459): `AutoTestRunner::detect == None` かつ `TesterCandidate::detect == Some` (Rust / Node / Python のいずれか) のとき、Act-mode の post-loop hook で main model を 1 回だけ同期呼び出し（`tools=None`、JSON-only、`<think>` strip + first JSON object 抽出、tool_calls 非空は abort）して smoke test を生成し、`tmp-tests/files/` に保存したうえで固定テンプレートの Bash（Rust: `cargo test --manifest-path`、Node: `node --check`、Python: `python3 -m py_compile`）を `TESTER_SMOKE_TIMEOUT_SECS=30` の明示 timeout 付きで実行する。Rust 経路は `state_root/sessions/<id>/tester-runs/<run_id>/` に transient harness（path dependency = workspace package、`tmp-tests/` とは独立 dir、DR1-009）を生成し、終了後に best-effort cleanup する。closure-based pure 関数 `run_tester_with_strategy<F, B, A>` + `Agent::try_invoke_tester` wrapper。`tester_called_this_turn` で per-turn cap = 1。`ANVIL_NO_TESTER` / Plan mode で disable。disable 順序は per-turn cap → Plan mode → `ANVIL_NO_TESTER` → no candidate（`check_invocation_gate` で production / E2E 共有 single source of truth）。承認モードは runtime 派生: `--yes` → `Auto`、TTY → `Interactive`（y/yes 以外 abort）、それ以外 → `Forbidden`。LLM reply は `validate_llm_reply` で空 / `<think>`-only / `>64 KiB` (`MAX_TESTER_LLM_REPLY_BYTES`) / `MAX_GENERATED_TESTS_PER_TURN=1` 超 / `tmp-tests/` 外 path / ASCII control char in path を防御 (`AbortReason::LlmMalformed` / `PathConfinementViolation`)。`build_command` は固定テンプレートで absolute `shell_quote`-escape、`classify_tester_outcome` は薄い分類器で `classify_auto_test` は流用しない (DR1-010)。`run_id` は `derive_run_id`(sha256 + timestamp) + `validate_run_id` で `run_[a-z0-9_-]{16,64}` allowlist 強制 (DR4-003)。Logging: `agent.tester.{llm_call_started,llm_call_completed,llm_call_failed,completed,failed,skipped}` を `llm-io.jsonl` に。`sanitize_tester_log` (`mask_secrets` + `truncate_for_log` cap=`TESTER_LOG_CAP=20_000`) で payload を防御 (DR4-006)。E2E は `tests/tester_skill_smoke.rs` に closure DI で 12 ケース (Ollama 不要、0.09s)。
- `src/agent/loop_run/auto_test.rs`
  - Watcher と connect する auto-test runner。Issue #459 で `pub(super)` 化された helper を Tester から流用する: `has_cargo_manifest(work_root)` / `package_json_has_test_script(work_root)` / `has_python_surface(work_root, &[String])` / `first_python_script(work_root) -> Option<PathBuf>` / `shell_quote(&Path) -> String` / `MAX_OUTPUT_BYTES = 12_000`。`classify_auto_test` は Tester では流用せず、Tester 側で独自の `classify_tester_outcome` を持つ (DR1-010)。Issue #457 で件数抽出 helper `pub(super) fn count_compile_errors(&AutoTestResult) -> Option<usize>` / `count_test_failures(&AutoTestResult) -> Option<usize>` を追加（plan 非依存の純関数、kind 分岐は turn.rs アダプタ側で集中）。マーカー pattern を module-private const `MARKER_CARGO_COMPILE_ERROR` / `MARKER_NPM_TSC_ERROR` / `MARKER_CARGO_TEST_FAILED` / `MARKER_PYTEST_FAILED_SUMMARY` に SSOT 化し、`classify_auto_test` と count helper の双方が参照する。`count_*` は heuristic 失敗時 `None` を返し（`Some(0)` で埋めない）、`i32::MAX as usize` 超は `None` に落とす。Issue #472 で `pub fn auto_test_disabled<F: Fn(&str) -> Result<String, VarError>>(get_env: F) -> bool` を追加（closure DI、`ANVIL_NO_AUTO_TEST` gate の SSOT、`loop_run.rs` 経由で integration test に公開）。
- `src/agent/loop_run/turn.rs`
  - Issue #457 で `build_anvil_test_summary(plan, &result) -> AnvilTestSummary` アダプタを追加し、`compute_anvil_score(.., None)` (turn.rs:3058-3059) を `Some(&summary)` に置換。AutoTestKind × passed の 4 アーム写像で `build_passed` / `tests_passed` / `compile_error_count` / `test_failure_count` を populate（Build 時 tests_passed は None / Test 時 build_passed は None という dimension 分離を厳守）。`Tester` / `NoVerifier` / `Skip` / `TransportError` 分岐では summary は None のまま（既存 #456 挙動を維持）。アダプタは agent → session 単方向依存の境界（`AnvilTestSummary` は session 層が定義する純粋値オブジェクト、`AutoTestResult` は session 層に流れない / DR3-002）。
  - Issue #463 で `try_inject_case_retrieval_message(&mut self) -> Option<RetrievalInjection>` adapter を追加（Issue #555 で戻り値を `Option<ConversationMessage>` → `Option<RetrievalInjection>` に変更）。`RetrievalInjection { message: ConversationMessage, selected_ids: Vec<String> }` は `pub(super)` module-scope struct で、選択された record の ID を photon mapper に渡す。message-build path で `working_memory_message` の直後に push して、Act mode の main prompt に `Relevant Local Cases:` セクションを注入する。adapter 内で (1) Plan mode → `agent.case_retrieval.skipped { reason="plan_mode" }` emit + cap 不消費、(2) per-turn cap 既消費 → skipped("per_turn_cap_consumed")、(3) `ANVIL_NO_CASE_RETRIEVAL` → cap 消費 + `agent.case_retrieval.disabled` emit、(4) `case_retrieval::retrieve_relevant_cases` 呼び出しで `Completed` / `Skipped` / `Err` を `agent.case_retrieval.{completed,skipped,failed}` に dispatch、の判定を完結させる (S3-008 layer 分離: SkipReason enum は session 層 3 値、log skipped.reason は adapter 含む 5 値)。`run_turn` 冒頭で `self.session.case_retrieval_invoked_this_turn = false` reset (`case_record_extracted_this_turn` の隣)。Logging 4 event の payload mask は既存 `mask_payload_inplace` (DR4-006) を SSOT として通る。`SessionSnapshot.case_retrieval_invoked_this_turn: bool` は `#[serde(skip, default)]` で per-turn cap flag (`case_record_extracted_this_turn` と同流儀)。Issue #555 で `try_inject_anti_pattern_message` も同様に `Option<RetrievalInjection>` に変更。Issue #556 で Photon Turn Hook を追加。`Agent` struct に `photon_context_pack_response: Option<String>` (per-turn フィールド、`handle_user_message` 冒頭で None reset) を追加。`run_turn` に pre/post hook を追加: Act mode でのみ `invoke_photon_context_pack()` (前) → `run_actor_loop()` → `invoke_photon_evaluate()` (後)。Plan mode ではどちらも skip。`build_request_messages` に `photon_context_pack_injection_message()` を DRY 1 回注入 (DR1-001)。純関数 `truncate_photon_context_pack(String) -> (String, bool)` と `build_photon_injection_message(Option<&str>, bool) -> Option<ConversationMessage>` を `pub` で公開、`MAX_PHOTON_CONTEXT_PACK_PROMPT_BYTES=8192`。E2E smoke: `tests/photon_turn_hook_smoke.rs` T1-T8 (Ollama 不要)。Issue #591 で `/v1/evaluate` adoption signal を完結。`Agent` struct に `last_adopted_summary_ids: Vec<String>` フィールド追加 (`pub(super)`、`Vec::new()` initializer、**reset は `handle_user_message` 冒頭であり `run_actor_loop` 冒頭ではない** — AS-01 / 設計判断 #2: `invoke_photon_evaluate` は post-loop 実行のため `run_actor_loop` 入口で reset すると空配列が送信される)。`invoke_photon_context_pack` は `render_context_pack` 戻り値 `RenderStats.adopted_summary_ids` を `self.last_adopted_summary_ids` に格納 (id は注入側で `sanitize_summary_id` 適用済、DR4-002)。`invoke_photon_evaluate` は (1) 純関数 `prepare_adopted_ids_for_evaluate(&[String], shadow_mode: bool) -> SanitizedAdoptedIds { list, truncated }` で再 sanitize → drop → `MAX_PHOTON_EVAL_ADOPTED_IDS=32` cap 適用 (DR4-NEW-001 evaluate-side defense-in-depth)、(2) `shadow_mode=true` のとき `summary_ids_adopted=[]` / `summary_ids_adopted_count=0` / `summary_ids_adopted_truncated=false` / `outcome=null` / `items_adopted_count=0` を強制 (DR4-NEW-002)、(3) 純関数 helper `derive_photon_feedback_outcome(&PhotonOutcomeInputs) -> Option<&'static str>` で outcome を allowlist (`"success"` / `"failure"` / `"safety_violation"` / `None`) から導出。`context_pack_event` JSON に 3 field 追加: `summary_ids_adopted` / `summary_ids_adopted_truncated` / `outcome`。`PhotonOutcomeInputs<'a> { last_feedback_kind / eligible_feedback_recorded_this_turn / anvil_score / adopted_id_count / shadow_mode }` 値オブジェクト経由で、free-text (feedback excerpt / command / path / model output) を helper 入力に取らない (DR4-NEW-004 secret 混入防止)。判定優先度は shadow → 0 adoption → safety_violation (`AnvilScore.unsafe_actions_blocked > 0` ∪ same-turn `FeedbackKind::UnsafeCommandBlocked`) → failure (9 variant allowlist: `is_eligible_for_reminder` から `UnsafeCommandBlocked` を除外、`is_eligible_failure_kind` SSOT、同一 turn flag guard) → success (`AnvilScore.user_visible_artifact=true`、DR3-NEW-002 で同一 turn flag に gating されない) → None。helper は `agent` 層に置く (DR4-NEW-003: photon 層には置かない)。`agent.photon_evaluate.completed` event payload を 4 → 8 keys 拡張: 既存 `session_id` / `turn_index` / `failed` / `duration_ms` + 新規 `summary_ids_adopted_count` / `outcome` / `adoption_status` / `summary_ids_adopted_truncated`。raw `summary_ids_adopted` 配列は event payload に出さない (DR4-NEW-004: 監査境界では count / static-allowlist string のみ通過)。`agent.photon_context_pack.completed` event payload は本 Issue で変更しない (AS-07 / 設計判断 #8: WF-09〜WF-13 / AR-01〜AR-08 / T1-T11 計 35 件の regression 保護)。E2E smoke: `tests/photon_evaluate_signal_smoke.rs` PES-01〜PES-05 + VR-07 / VR-09 / VR-10 (8 ケース、mockito `match_request` で request body capture、Ollama 不要) + Module unit tests: `derive_photon_feedback_outcome_tests` 11 件 (case A-F truth table) / `prepare_adopted_ids_for_evaluate_tests` 7 件 (sanitize + cap + shadow guard)。
- `src/git/checkpoint.rs`
  - checkpoint / rollback

## Development Expectations

- 旧アーキテクチャへ戻す方向の継ぎ足しはしない
- provider abstraction を増やさない
- local LLM 向けの prompt / loop の単純さを優先する
- 新機能は E2E 寄りの検証を伴わせる

## Release Process

変更しない。

- package / binary は `anvil`
- `.github/workflows/ci.yml` は `fmt` `clippy` `test` `build`
- `.github/workflows/release.yml` は tag push 時に gzip artifact を作って GitHub Release を切る
