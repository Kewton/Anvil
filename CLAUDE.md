# CLAUDE.md

毎会話の context に load される設計指針。**ルールと不変条件のみ**を記述する。
詳細仕様 / 実装履歴 / API spec は記載しない（コード / git log / dev-reports が SSOT）。

## Repo Intent

Rust 版 local-first coding agent (Ollama 専用、`workspace/v0.1.0` ベース)。
旧 Anvil 拡張を継ぎ足すのではなく、vibe-local 的な小さい実装に振り切る。

## File Map

- `src/photon/*` — Photon サイドカー HTTP 連携 / context_pack レンダラー / adoption signal
- `src/agent/loop_run/*` — エージェントループ / turn 制御 / 各種 confirm skill (work_mode / feedback_kind / quality / photon_user_feedback)
- `src/agent/loop_run/actor_loop_flow.rs` — Issue #681 (parent #680, Phase 1): actor loop 制御フローのデータ型を `turn.rs` から抽出。`PostReplyRecoveryArgs/Outcome` + 27 個の `ActorLoop*Args` / `ActorLoop*Outcome` struct/enum (pre-reply / tool-preparation / no-tool / task-contract-reply / completion / post-tool / missing-repo-change / empty-reply / prose-only / plan-tool-followup) + `TaskContractVerifierFlowOutcome` + 2 つの Outcome constructor (`repair_job_done_outcome` / `missing_repo_edits_finalize_outcome`) を保持。`pub(super)` 限定、facade 再 export なし (DR3-001)、`turn.rs` が唯一の in-crate consumer。`impl Agent` method の free-function 化は後続コミットで漸進実施 (既存 "Split X helpers" 粒度)。
- `src/agent/loop_run/verifier_orchestration.rs` — Issue #682 (parent #680, Phase 2): verifier orchestration の 7 データ型を `turn.rs` から抽出。`JobInstallOutcome` (PR-001 SSOT) / `VerifierDiagnosticPassOutcome` (Accepted / RetryPending / Unavailable / Skipped) / `PreparedVerifierDiagnosticPass` / `PreparedVerifierRepairPass` / `VerifierRepairAttemptProgress` (Return / Continue / Break trichotomy) / `StructuredTaskContractVerifierRun` / `TaskContractVerifierFlowArgs<'a, 'b>` を保持。`pub(super)` 限定、facade 再 export なし (DR3-001)。`turn.rs` 内 dispatch method (`run_verifier_diagnostic_pass` / `verifier_repair_pass` / `drive_task_contract_verifier` / `record_controller_verifier_repair_invalid` / `emit_safe_stop_report_for_repair_exhausted`) は後続 PR で漸進的に migrate (Phase 1 / actor_loop_flow パターン踏襲)。`actor_loop_flow.rs` は `super::verifier_orchestration::TaskContractVerifierFlowArgs` 経由で参照。
- `src/agent/loop_run/scaffold_pipeline.rs` — Issue #683 (parent #680, Phase 3): scaffold / deterministic-scaffold pipeline データ型 + telemetry event 名定数 + 全 dispatch method を `turn.rs` から抽出 (batches 1-17 完了)。`ScaffoldFramework` (Next / React / Nuxt) + `impl { label, scaffold_hint }` / `PlanExplorationKey` (stage / tool_name / normalized_args triple、`actor_loop_flow` の repeated-exploration detector 用) / `ScaffoldFallbackResult` (NotApplicable / Applied / Failed / Skipped) / `DeterministicScaffoldSpec` (label / event / scaffold_kind / files) / `ScaffoldDiffStatus` (NotScaffold / Changed / UnchangedOrMissing) を保持。`EVENT_DETERMINISTIC_FASTAPI_SCAFFOLD` / `_PYTHON_CLI` / `_FORMAT_ERROR_SMALL_EDIT` / `_PYTHON_TEST_FALLBACK` / `CREATE_NEXT_APP_PACKAGE_VERSION` を `pub(super) const` で SSOT 化。全 scaffold dispatch fn (`maybe_apply_deterministic_nextjs_scaffold` / `maybe_materialize_{mode,task_contract,framework_game,python_test}_fallback` / `maybe_apply_deterministic_{quality,polish}_fallback` / `_after_timeout` / `maybe_apply_local_llm_small_edit_fallback` / `materialize_deterministic_fallback_plan` / `maybe_apply_deterministic_edit_after_format_error` / `maybe_fallback_plan_model_after_timeout` / `maybe_apply_requested_port_script` / `maybe_apply_deterministic_framework_support_files` / `empty_workspace_scaffold_policy_error` / `post_scaffold_{edit,continuation}_recovery_{message,target}` / `repo_edit_has_post_scaffold_delta` / `scaffold_candidate_for_missing_role` / `active_task_{requires_nextjs_scaffold,requested_scaffold_framework}` / `python_csv_names_from_request_and_anvil` / `mode_deterministic_scaffold_spec` / `write_deterministic_scaffold_files` / `finalize_deterministic_scaffold_materialization` / `record_scaffold_artifact_snapshot` / `deterministic_nextjs_scaffold_skip_reason` ほか) を `pub(super) fn name(agent: &/&mut Agent, ...)` 形式で保持。caller は `turn.rs` / `actor_loop_flow.rs` / `verifier_orchestration.rs` / 本 module 内の sibling 呼び出し (`super::scaffold_pipeline::*`)。`pub(super)` 限定、facade 再 export なし (DR3-001)。`impl Agent` には scaffold veneer shim を残さない。
- `src/agent/loop_run/reminder_pipeline.rs` — Issue #684 (parent #680, Phase 4): reminder dispatch orchestration を `turn.rs` から完全抽出 (batches 1-3 完了)。`ReminderCallContext` struct + `impl { inputs() -> ReminderInputs<'_>, log_outcome(turn_index, &outcome, include_inputs) }` + 全 dispatch fn (`maybe_invoke_reminder` / `reminder_feedback_kind` / `reminder_gate` / `prepare_reminder_context` / `reminder_mode_label` / `active_precautions_at_call_time` / `try_clone_reminder_client` / `run_reminder_sidecar`) を `pub(super) fn name(agent: &/&mut Agent, ...)` 形式で保持。caller は `turn.rs` (内部メソッド経路なし) / `actor_loop_flow.rs` (`super::reminder_pipeline::maybe_invoke_reminder(agent, ...)` 直呼出)。`reminder.rs` sibling の public SSOT (`ReminderInputs` / `ReminderOutcome` / `build_log_payload`) は変更しない。`pub(super)` 限定、facade 再 export なし (DR3-001)。`impl Agent` には reminder veneer shim を残さない。
- `src/agent/loop_run/streaming_reply.rs` — Issue #685 (parent #680, Phase 5): streaming reply render state + chunk-handling flow を `turn.rs` から抽出。`StreamingReplyRenderState { first_chunk, renderer: Option<MarkdownRenderer> }` struct + `impl { new() }` (markdown disabled 時 `renderer = None` / TTY color + utf-8 capability 検査) + `streaming_reply_needs_prefix` / `streaming_reply_needs_trailing_newline` 純関数 predicate + `handle_streaming_assistant_chunk(render_state, chunk, stream_output, stop_signal, interrupt_flag)` / `finish_streaming_assistant_reply(render_state, stream_output)` (旧 `impl Agent` 関連 fn を free fn 化、`&Agent` 不要) を保持。`turn.rs::stream_assistant_reply` が `super::streaming_reply::*` 直接呼出で消費。`pub(super)` 限定、facade 再 export なし (DR3-001) — `turn.rs` が唯一の in-crate consumer。
- `src/agent/loop_run/project_verifier.rs` — `ProjectVerifier` capability (verifier_repair cheap check SSOT, Issue #639). private mod, not re-exported (DR3-001) — `turn.rs` is the only in-crate consumer.
- `src/agent/loop_run/semantic_failure.rs` — Issue #647 Phase A.1: `SemanticFailureReport` schema + `build_failure_cluster_from_observation` + `cluster_key` (deterministic, sanitize-then-shape-then-hash). private mod, not re-exported (DR3-001).
- `src/agent/loop_run/spec_authority.rs` — Issue #647 Phase A.2: `SpecAuthority` enum (5 variant) + `select_authority` + `ArtifactConsensus` tie-break + `detect_test_weakening` / `detect_impl_weakening` (9 pure-fn detectors). private mod, not re-exported (DR3-001).
- `src/agent/loop_run/auto_test.rs` — Issue #651: `VerifierCommand` (allowlisted constructor, private fields) + `OwnedTestVerifierPlan::{Runnable, Weak, Missing}` enum + `detect_with_owned_test_artifacts` / `validate_bound_test_artifacts_for_execution` / `run_structured` (spawn + concurrent stdout/stderr drain + polling timeout/kill). `pub(super)`、`loop_run.rs` から re-export しない (DR3-001). Issue #661: `#[non_exhaustive] enum RunnerKind { Cargo, Python3 }` + `as_str()`/`from_runner_str()` + `PathHashHex` newtype (`from_relative_str` 経由のみ構築、raw path 型ガード) + `HermeticEnvPlan` / `HermeticEnvSummary` / `build_hermetic_env_plan` (Phase B: `env_clear` + `filter_env_for_tester` 経由 allowlist 再注入 + Python extras + PYTHONPATH 制御 + cwd 固定、DR1-009 SSOT 直接参照禁止) + `VerifierInvokedSnapshot` (bound_artifacts 上限 `VERIFIER_INVOKED_BOUND_ARTIFACTS_CAP=16` truncate) + `validate_bound_test_artifacts_for_execution` を `classify_ownership(enabled())` SSOT に切替 + `evaluate_pythonpath_with_separator` 純関数 (cross-platform PYTHONPATH 検査) + `detect_external_imports_in_output` (`DetectedExternalImports { entries, total_count, truncated }`、`EXTERNAL_IMPORT_DETECTED_CAP=8`) + `path_is_known_safe_external` (canonicalized HOME/CARGO_HOME/RUSTUP_HOME anchor + component-boundary check) + `sanitize_path_for_runner_resolution` + `resolve_runner_in_sanitized_path` (DR4-001 PATH hijack fail-closed) + `VerifierCommand::new_allowlisted` の leading-dash reject (DR4-002).
- `src/agent/loop_run/artifact_ownership.rs` — Issue #651: `owned_test_artifacts(artifacts, work_root, scope, edited_this_session_for, scaffold_changed_for)` ヘルパ (SSOT)。内部で `classify_ownership` を必ず通し `Owned` のみ採用、`ignored top dir` 拒否。Issue #661: `pub(super) struct NestedTestAdmission(bool)` newtype + `disabled()` / `enabled()` const fn + `Default = disabled()` (DR1-003 type-level mis-propagation guard) + `OwnershipInputs.nested_test_admission` field + `classify_ownership` の no-signal nested test branch (admission `enabled()` 時のみ `app/tests/...` を Owned 昇格、`nearest_existing_ancestor_within_work_root` で symlink containment 保証、CB-003 fix)。4 経路 (`record_repo_edit_event` / `record_verifier_observation` / `validate_bound_test_artifacts_for_execution` / `seed_artifact_ledger_verifier_observation`) のみ `enabled()`、その他 caller (`repair_job` / `collect_meaningful_workspace_files` / write enforcement) は `default()` 維持。
- `src/agent/loop_run/task_contract.rs` — Issue #651: `CompletionDecision::SafeStop { reason: SafeStopReason }` variant + `ArtifactRecoveryAction::SafeStop { reason }` variant + `evaluate_with_owned_test_artifacts` gate (`test_execution_required=true && owned_test_artifacts.is_empty()` → `SafeStop`)。既存 `evaluate(&evidence)` は `EvaluateMode::Legacy` で gate を bypass (regression guard). Issue #661: `has_bound_build_test_verifier` の Done gate を `Some(n) if n > 0` のみ true 化 (`None` / `Some(0)` 拒否、`py_compile` のみの done 不可) + `EvaluateMode::OwnedTestArtifacts { weak_metadata: Option<OwnedTestVerifierWeakMetadata> }` struct variant + `done_gate_safe_stop_reason` pure-fn (5 mapping pattern: `is_empty()` → `VerifierMissing` / `Some(0)` → `VerifierWeak` / `None+Weak metadata` → `VerifierWeak` / `None+no-metadata` → `VerifierMissing` / `Some(n>0)` → Done) + `evaluate_with_owned_test_artifacts_and_weak_metadata` 新 public API。Legacy bypass 経路 (`evaluate(&evidence)`) は完全維持。Issue #664: has_required_setup_artifact / has_optional_setup_or_verifier_prerequisite / VerifierPrerequisiteSignal (DR1-002 SSOT、#663 forgeability-safe pattern 踏襲、pub(super))
- `src/agent/loop_run/repair_attempt_outcome.rs` — Issue #653: `RepairAttemptOutcome` lifecycle ledger (`RepairAttemptOutcomeKind` 5 variant / `RepairRejectionKind` 2 variant / `should_promote_to_exhausted_after_push` pure-fn / `MAX_REPAIR_ATTEMPT_OUTCOMES = 16`). Issue #662: `#[non_exhaustive]` 新規付与 + 新 3 variant additive (`RejectedMalformed` / `RejectedNoop` / `RejectedDuplicate`) + `PromotionBucket` private enum (5 variant) + `should_promote_to_exhausted_after_push` を 2 述語合成 (`should_promote_unsafe` 既存 SSOT + `should_promote_non_unsafe` bucket-based)、`AppliedNoProgress` / `AppliedWorsened` / 新 3 variant が `count >= 2` で promotion。`AppliedImproved` / `RejectedNoCandidate` は除外維持。private mod, not re-exported (DR3-001)。
- `src/agent/loop_run/artifact_completion_job.rs` — Issue #652: `ArtifactCompletionJob` / `ArtifactAttemptOutcome` (4 variant: WrongTarget / NoTool / ProseOnly / RolePolicyViolation) / `ArtifactCompletionFailureSnapshot` + role-specific retry budget (`ARTIFACT_COMPLETION_ATTEMPT_LIMIT=3`) + projection to `RecoveryTarget` / `EffectiveToolPolicy::artifact_directed`. Sanitize pipeline `mask_secrets` + length cap + control-char neutralize + `mask_payload_inplace` final-defence line. private mod, not re-exported (DR3-001). `turn.rs` is the only behavioral in-crate consumer. Issue #663: `ArtifactCompletionStatus` を 5 variant に拡張 (`PendingTarget` / `AwaitingEdit` / `EvidenceObserved` / `Satisfied` / `Exhausted { reason }`); parent enum は `#[non_exhaustive]` を付与せず exhaustive match を維持 (DR1-004)、子 `ExhaustedReason` のみ `#[non_exhaustive]` (DR1-003 / OCP、`BudgetExceeded` 1 variant)。Satisfied 遷移の SSOT は `record_satisfied_from_ledger(&RequiredArtifactsProjection)` (AD2、status guard は本 fn 内 1 箇所に集約 / DR1-001)。turn.rs の chokepoint `refresh_artifact_completion_satisfied` は projection 取得 + 委譲のみで pre-filter なし。`record_repo_edit_observed()` で AwaitingEdit → EvidenceObserved 遷移を追加。`record_attempt` は `AwaitingEdit` / `EvidenceObserved` のみ budget 減算 (PendingTarget → Exhausted 直接遷移は型レベル不可、R5)。`maybe_install_artifact_completion_job_for_hint` / `build_arbiter_candidates::ArtifactRecovery` から `hint.role != Test` early-return を撤去 (Phase C / AD5) — Implementation / UsageDocs / Setup でも `EffectiveToolPolicy::artifact_directed_from_job` を選択。legacy `record_completion()` は `#[cfg(test)]` + `#[deprecated]` で production build から消去 (Codex CB-005 fix)、Satisfied 遷移の唯一の production SSOT は `record_satisfied_from_ledger`。`ArtifactCompletionReport.budget_state` は 5 state + role + attempts + `target_path_hash` (stable_path_hash 経由) + `projection_overflowed` の additive JSON projection (`PAYLOAD_SCHEMA_VERSION = 1` 不変 / AD6)。`turn.rs::build_arbiter_candidates::ArtifactRecovery` は `current_artifact_recovery_target.is_some() && artifact_completion_job.is_none()` のとき write-capable policy を生成しない (Codex CB-001 fix、`artifact_directed_from_job` 経由のみ write 権限)。`turn.rs::collect_recent_action_labels` は `Read / Write / Edit / Bash` の `path` / `command` 引数を `stable_path_hash(mask_secrets(...))` 経由の 16-hex correlator に置換 (Codex CB-004 fix、SafeStopReport.actual_actions の raw path / raw command 流出を type-level に防止)。`job_report.rs::snapshot_safe_stop_linkage` は固定 priority order (ArtifactCompletionFailed → VerifierFailedSafeStop → VerifierWeak → VerifierMissing → DiagnosticTargetMissing → RepairExhausted) で linkage reason を選択 (Codex CB-003 fix、`HashSet::iter().next()` の非決定性を排除)。Issue #664: attempt_outcome_to_json_value (pub(super) projection、mask_secrets + stable_path_hash 16-hex correlator、AD5/AD10)
- `src/agent/loop_run/artifact_ledger.rs` (Issue #663 追記) — Issue #663: forgeability-safe `RequiredArtifactsProjection { values: BTreeMap<ArtifactRole, bool>, overflowed: bool }` newtype と `required_artifacts_completed_projection(&TaskContract) -> RequiredArtifactsProjection` accessor を追加 (DR1-002 / DR1-009 / DR4-001)。constructor は本 module 内に閉じ、外部 caller (`artifact_completion_job`) は `is_satisfied(role)` / `overflowed()` accessor のみで参照。ledger overflowed=true 時は全 role `false` の fail-closed (R1)。既存 `required_artifacts_completed (-> BTreeMap)` は ledger 内部互換性のため当面残置 (後続 Issue で置換予定)。
- `src/agent/loop_run/safe_stop_e2e_tests.rs` — Issue #654: in-crate `#[cfg(test)] mod` for `agent.safe_stop.report` event 6 stop_reason E2E (artifact_completion_failed / verifier_failed_safe_stop / verifier_weak / verifier_missing / diagnostic_target_missing、Issue #662 で `repair_exhausted` を追加し 5→6 拡張、`DOCUMENTED_FAILURE_TYPES` も 7→8 へ `"repair_exhausted"` 追加)。production binary には含まれない (DR3-001 / CB-001 fix)。`repair_job.rs` の `StopReason` (Issue #662 で `#[non_exhaustive]` 新規付与 + `RepairExhausted` variant additive 追加 + `PromotionResult { promoted, all_clusters_exhausted }` struct + `record_repair_attempt_outcome -> PromotionResult` signature + `debug_assert!(semantic_plan.is_some())` precondition + `SafeStopReport::build_from` 内 `failure_type` 上書き分岐拡張 (`RepairExhausted` → `VerifierFailureType::RepairExhausted`)) / `DiagnosticTargetMissingReason` / `ExhaustedAttemptsSummary` / `SafeStopInput` / `SafeStopReport` / `safe_relative_path_string` / `build_actual_actions` / `select_diagnostic_target_missing_reason` も同 Issue で追加（pub(super)、`turn.rs` のみが in-crate consumer）。`turn.rs::build_safe_stop_payload` (純関数、4KB hard cap) + `record_safe_stop_report` (薄い shell、`Agent.safe_stop_report_emitted: HashSet<StopReason>` で per-turn dedup) も同 Issue。Issue #662 で `turn.rs::emit_safe_stop_report_for_repair_exhausted` shell + `loop_run.rs::emit_safe_stop_report_repair_exhausted_for_test` test seam (CB-001 pattern) + 2 production caller (`turn.rs:drive_task_contract_verifier` Applied 系 + `turn.rs:record_controller_verifier_repair_invalid` Invalid 系) が `PromotionResult.all_clusters_exhausted` を観察し emit。`turn.rs::RepairRejectionSignal` (Noop / Duplicate / Malformed) + `ValidationFailure.rejection_signal: Option<...>` field + `build_verifier_repair_pass_ledger_outcome(weakening, rejection_signal, semantic_plan)` で 5-4-1 priority SSOT (Malformed → Noop → Duplicate → Unsafe → Applied) を実装。検出経路は `validate_verifier_repair_intents` (per-intent `old_string==new_string` + post-apply `contents==original_contents` + fingerprint duplicate) + parse error map (Malformed)、record entry point は既存 `record_repair_attempt_outcome` 単一 (DR2-005)。
- `src/agent/loop_run/artifact_ledger.rs` — Issue #659: turn-local append-only `ArtifactLedger` SSOT for current-turn artifact observations. `ArtifactOrigin` 3-variant (`#[non_exhaustive] Existing / Scaffold / RepoEdit`) + `LedgerAdmissionContext { work_root, scope }` + `classify_ownership` SSOT 経由の admission rule (workspace-relative + `ancestor_within_work_root` symlink containment + length cap + role re-confirm)。Issue #661: `admit_event` / `admit_path_only` / `record_internal` シグネチャに `nested_test_admission: NestedTestAdmission` 引数追加 + `record_repo_edit_event` / `record_verifier_observation` 経路で `enabled()` 渡し (nested test path の Owned 昇格を verifier-binding SSOT として有効化)。4 record helpers (`record_existing_event` / `record_scaffold_event` idempotent baseline、`record_repo_edit_event` per-observation append、`record_verifier_observation` upsert)、bounded by `MAX_ARTIFACT_LEDGER_EVENTS=256` / `MAX_ARTIFACT_LEDGER_PATH_BYTES=4096` / `MAX_ARTIFACT_LEDGER_OBSERVATIONS=256` (cap 超過は新規 reject + `overflowed=true`)。3 projection (`owned_test_artifacts` / `required_artifacts_completed: BTreeMap` / `active_job_candidates` declaration-order stub) は `overflowed=true` 時 fail-closed。Observability は `agent.artifact_ledger.{event_recorded,turn_summary,divergence_detected}` 3 event を `mask_payload_inplace` 最終防衛線で emit、raw path 非出力 (`path_hash` のみ)。`Agent::artifact_ledger` field は `turn.rs::handle_user_message` 冒頭で `clear()` (per-turn rule、`SessionSnapshot` 不所持)。dual-source divergence assertion (`#[cfg(debug_assertions)]`) は legacy `turn_edited_relative_paths` を authority とする write-through adapter。`task_contract_artifact_states` / `owned_test_artifacts_for_verifier` の内部実装は ledger を並行計算 (adapter 期間中は legacy authority、divergence 時 legacy 結果)。test seam は `pub(crate) fn seed_artifact_ledger_repo_edit_for_test(agent, path)` (旧 `seed_turn_edited_relative_path_for_test` は本 Issue で削除)。private mod, not re-exported (DR3-001)、`turn.rs` が唯一の in-crate consumer。policy 型 (`CompletionDecision` / `ArtifactRecoveryAction` / `EffectiveToolPolicy` / `RepairJob` / `VerifierCommand`) を import しない。後続 #660/#661/#663 が同 projection を読む signature anchor。
- `src/agent/loop_run/job_report.rs` — Issue #666: agent-layer structured job report types + emit chokepoint. `pub(super) trait JobReport` (`EVENT_NAME` / `PAYLOAD_SCHEMA_VERSION` no-default / `dedup_key`) + 4 Report struct (`ArtifactCompletionReport` / `VerificationReport` / `RepairReport` / `MemoryReport`) + `SafeStopLinkage` DRY sub-struct (3 Report embed, Memory omits per DR1-004) + `build_envelope<R>` helper (fail-closed Null fallback + `tracing::error` observability) + `enforce_bounds` (1 KiB field cap / 8 KiB envelope cap with `truncated`/`overflowed` flags). `impl Agent` exposes `record_job_report<R>` (Option<Value> chokepoint with `{event_name}::{dedup_key}` HashSet dedup, session_id injection, log_llm_event move + clone-for-caller), `persist_job_report_to_session` (best-effort SystemTime/UNIX_EPOCH ms + `mask_payload_inplace` defensive re-pass before `SessionStore::append_job_report`), and `maybe_emit_job_reports` (single-site builder, called from `turn.rs::handle_user_message` after `run_turn`). private mod, not re-exported (DR3-001); `turn.rs` only in-crate consumer.
- `src/agent/loop_run/job_report_e2e_tests.rs` — Issue #666: in-crate `#[cfg(test)] mod` E2E suite (CB-001 fix pattern, `safe_stop_e2e_tests.rs` precedent). `pub(crate) fn maybe_emit_job_reports_for_test` seam in `loop_run.rs` lets the test mod drive the chokepoint without widening production visibility. 5 test cases cover 4-event emit, payload schema v1, per-turn dedup, persisted JSONL records (schema_version=1 envelope), Memory asymmetry (no SafeStopLinkage). Production binary excludes this mod.
- `src/agent/loop_run/bash_policy_e2e_tests.rs` — Issue #664: in-crate #[cfg(test)] mod E2E suite (CB-001 fix pattern、safe_stop_e2e_tests precedent)。production binary 不含。受入条件 (l) / (g) / (h) + Stage 4 security (DS1-001/002/003) + AD20/AD22 fail-closed + Phase 1-4 helper references で dead_code 解消。
- `src/agent/loop_run/pam_advisory.rs` — Issue #667: PAM (photon-action-memory) advisory adapter SSOT。private mod、`pub(super)` 限定、`loop_run.rs` から `pub use` 禁止 (DR3-001)。`MAX_PAM_DECISION_LIST_LEN = 16` 専用 SSOT (DR1-010、他 16-cap と非共有)。`#[non_exhaustive] enum PamAdvisoryMode` (Shadow / Live / Suppressed、DR1-004) + `#[non_exhaustive] enum SuppressionReason` (RoleMismatch / ImplExpansionBlocked / BehaviorNonGoalMismatch、DR1-009) + `struct SuppressedSummary` + `struct ShadowVsLiveDiff` (DR1-006 `Option` で `Some` 即 shadow) + `struct PamAdvisoryDecision` (mode / injected / suppressed / shadow_vs_live_diff / active_job_role 5 field) + `struct PamAdvisoryOutcome { decision, live_admitted_views }` (DR3-001: report + live filter 両方) + `struct PamAdvisoryDecisionPayload` (DR1-011 serde SSOT、`serde_json::json!` マクロ非使用、`skip_serializing_if = "Option::is_none"` で shadow turn のみ shadow_vs_live_diff 出力) + `struct PamAdvisoryModeInput { shadow: bool }` (DR1-002、`Config` 直 import 回避) + `struct PamAdvisoryInputs { role_hint, behavior }` (DR1-008、S3-008 (1)(2)(3) 解決 view)。pure-fn `evaluate_pam_advisory(context_pack, blocked_ids, active_job: Option<&ActiveJobSelection>, current_role_hint, behavior, mode_input) -> PamAdvisoryOutcome` (DR2-001 `Option<&_>` 入力契約、DR2-003 path-a fail-open) + `classify_summary` (4 シグナル決定木: path classify / role mismatch / active job kind / behavior non_goal) + `derive_mode` (S5-003 precedence) + `format_active_job_role` (module-private、DR1-003 SSOT) + `job_kind_to_artifact_role` (DR2-002 / DR3-003: 全 variant `None`、`ArtifactRecovery` は `artifact_completion_job.role()` SSOT 委譲) + 純関数 helper (`apply_cap_strings` / `apply_cap_suppressed` / `infer_role_from_view` / `label_matches_role`)。adapter は `sanitize_summary_id` を直呼びせず、`enumerate_admitted_items_with_provenance` の sanitized `provenance.summary_id` から `suppressed_summary_ids` を集合差分で算出 (DR1-007 SSOT 1 本化)。`Agent::record_pam_advisory_decision` thin shell (DR1-005、`pub(in crate::agent::loop_run)`、`loop_run.rs::impl Agent` 配置 DR2-009) は `config.pam_advisory_enabled` gate + `last_pam_decision_this_turn.is_some()` 早期 return + `pam_advisory_inputs` 1 度のみ取得 + `last_pam_decision_this_turn = Some(decision)` の 1 set で完結。caller は (path-a) `turn.rs::invoke_photon_context_pack` の `enumerate_admitted_items_with_provenance` 後 `photon_context_pack_response` 書込前 + (path-b) `build_request_messages` 成功 fetch 直後 (DR1-001 `let _ = ...` discard 解消、shadow turn は renderer 入力 cache に流さない) の 2 chokepoint。`MemoryReport.pam_decision` 投入は `job_report.rs::maybe_emit_job_reports_with_linkage` の MemoryReport block で `last_pam_decision_this_turn.is_some()` OR + `to_json_value` 経由、`mask_payload_inplace` を最終防衛線として通過 (DR3-001 / Security Invariants)。`Agent.last_pam_decision_this_turn` は `turn.rs::handle_user_message` 冒頭で reset (per-turn rule、`SessionSnapshot` 不所持)。in-crate `#[cfg(test)] mod pam_advisory_e2e_tests` (CB-001 / DR3-001、production binary 不含) + Tier-1 seam `pub(crate) fn pam_advisory_decide_for_test` + Tier-2 seam `pub(in crate::agent::loop_run) fn record_pam_advisory_decision_for_test` (DR2-004)。unsafe 不使用。
- `src/agent/loop_run/pam_advisory_e2e_tests.rs` — Issue #667: in-crate `#[cfg(test)] mod` E2E suite (CB-001 fix pattern、`safe_stop_e2e_tests.rs` / `job_report_e2e_tests.rs` precedent)。production binary 不含 (DR3-001)。T1-T3 (mode precedence shadow/suppressed/live) / T7 (per-list cap truncate) / T8 (adapter は `JobCandidate` を返さない type-level / Agent 状態を変えない) / T9 (`pam_advisory_enabled = false` bypass) / T11 (`sanitize_summary_id` SSOT 経由 / raw id 流入なし) / T12 (renderer security filter で落ちた item が PAM decision id list に出ない) / T13 (shadow turn MemoryReport emit + `pam_decision.mode = "shadow"`) / T14 (per-turn dedup) / T15 (safe_stop linkage と同 turn 共存) / T16 (`mask_payload_inplace` 通過後 raw 不在) / T17 (PamAdvisoryDecisionPayload schema-pin / `PAYLOAD_SCHEMA_VERSION = 1` 不変) / T18 (既存 `agent.memory.report` event 不変 regression) / T23 (blocked_ids parity DR4-001) を網羅。
- `src/agent/loop_run/required_behavior.rs` — Issue #635 で導入された `RequiredBehaviorContract` (operations / domain_terms / interface_hints / required_artifacts / verification / confidence: f32 / test_execution_required: bool) を Issue #665 が 4 新フィールド (`behavior_goal: Option<BoundedLabelWithExcerpt>` / `required_capabilities: Option<Vec<BoundedLabelWithExcerpt>>` / `verification_expectations: Option<Vec<BoundedLabelWithExcerpt>>` / `non_goals: Option<Vec<BoundedLabelWithExcerpt>>`) で拡張。`BoundedLabelWithExcerpt { label, excerpt: Option<String> }` private struct + `SchemaError::{LabelTooLong, ExcerptTooLong}` variant 追加。`Default` 禁止 (DR2-006) で literal 構築サイト 10 箇所 (production 2 + tests 7 + `artifact_ledger.rs::test_required_behavior` 1) を compile-time 強制更新。`extract_behavior_goal` / `extract_non_goals` で raw excerpt 付き bounded label を抽出、`required_capabilities` / `verification_expectations` は post-filter `operations` / `domain_terms` / `verification` からの derive (S5-003、`excerpt: None` 原則)。`filter_against_request` は derived field を candidate から信頼せず post-filter source で再生成 (S5-002 canonicality)。`BehaviorContractProjection { confidence, fields_used, behavior_goal, required_capabilities, verification_expectations, non_goals }` + `project_behavior_contract(&TaskContract) -> Option<BehaviorContractProjection>` (sidecar accessor、`confidence < LOW_CONFIDENCE_THRESHOLD = 0.5` または `fields_used.is_empty()` で `None`)。`BehaviorProjectionEventKey { schema_version, consumer, confidence_bucket, fields_used }` は payload-shaped dedup key (raw label / excerpt を保持しない / S5-006)。`behavior_contract_projected_payload` は `agent.behavior_contract.projected` 用 versioned envelope (`schema_version: 1` / S7-002) を生成、raw label / excerpt は payload に含めない。`MAX_BEHAVIOR_CONTRACT_PROJECTION_BYTES = 1200` cap は `turn.rs::behavior_contract_payload_value` が low-priority field drop + `truncated=true` で適用 (S7-003)。`FIELDS_USED_ORDER` deterministic SSOT。`pub(super)` 限定 / facade 再 export なし (DR3-001)。`turn.rs::verifier_diagnostic_messages` / `verifier_repair_diagnostic_pending_note` (system note は metadata のみ / S5-005) / `verifier_repair_pass_messages` の signature に `behavior_projection: Option<&BehaviorContractProjection>` 引数追加、production caller (`run_verifier_diagnostic_pass` / `verifier_repair_pass` / `push_verifier_repair_recovery_note` / repair policy note) が caller-side projection build → 参照渡し。`#636` completion gate (`excerpt_hits_any_operation` / `excerpt_hits_any_domain_term` / `behavior_coverage_enabled` / `excerpt_satisfies_behavior`) は本 Issue で変更なし、regression guard test 2 件 (task_contract / required_behavior 両側) で invariance 固定。
- `src/agent/loop_run/behavior_contract_projection_e2e_tests.rs` — Issue #665 Phase 8 in-crate `#[cfg(test)] mod` (CB-001 / S7-001、`src/agent/loop_run.rs` から明示 mod 登録、production binary 不含)。受入条件 11 件分の E2E test cases: low confidence regression / high confidence consumption / pending note data boundary / derived field canonicality / prompt data boundary (malicious label escape) / observability event payload exclusion / prompt budget cap / event key dedup payload-shaped / completion gate invariance / release build exclusion / framework neutrality static regression。`FIELDS_USED_ORDER` canonical anchor 確認も追加。
- `src/agent/loop_run/active_job_arbiter.rs` — Issue #660: action selection SSOT for write owner arbitration. `#[non_exhaustive] enum ActiveJobKind` (5 selectable: VerifierRepair / ForcedSmallEditRecovery / ArtifactRecovery / FocusedEditRecovery / LocalLlmSmallEditAfterRead) + `#[non_exhaustive] enum DesiredAction` (4 variant) + `enum Budget { Unbounded, Bounded { attempts_used, attempts_limit: NonZeroU32, exhausted_stop_reason: StopReason } }` + `struct JobCandidate { kind, desired_action, policy: EffectiveToolPolicy, budget }` + `struct ActiveJobSelection { selected: Option<JobCandidate>, rejected: Vec<RejectedJob> }` + `#[non_exhaustive] enum RejectionReason` (LowerPriority / BudgetExhausted)。pure function `select_active_job(&[JobCandidate]) -> ActiveJobSelection` で priority 1-5 順序 + bounded budget exhaustion を決定。`project_policy(&ActiveJobSelection) -> EffectiveToolPolicy` は selected の policy clone (None なら `unrestricted()`)。private fn `stable_path_hash` は `artifact_ledger.rs` と同一の 16-hex DefaultHasher (非暗号学的 correlator、SSOT は doc comment で拘束)。`turn.rs::effective_tool_policy()` は AnswerOnlyMode + Plan mode の pre-arbitration gate を early-return した後、`build_arbiter_candidates()` → `select_active_job` → `project_policy` の薄い shell。Plan mode の plan-file Write/Edit 例外 authority は `registry.rs::resolve_plan_mode_write_target` / `enforce_plan_stage_scope` が保持 (#660 CB-001 fix)。Observability は `agent.active_job.selected` 単一 event を per-turn diff-based dedup (`Agent.last_active_job_selection`) で emit、payload は `{ iteration_seq, selected, rejected[], policy_projected, budget_state }` (raw path は `stable_path_hash(mask_secrets(...))`、raw command は redaction)、`log_llm_event` 経由で `mask_payload_inplace` 最終防衛線通過。`last_active_job_selection` は `turn.rs::handle_user_message` 冒頭で reset (per-turn rule、`SessionSnapshot` 不所持)。caller 6 site signature は本 Issue 全 4 Phase で不変。private mod, not re-exported (DR3-001)、`turn.rs` が唯一の in-crate consumer。unsafe 不使用。`active_job_candidates` projection (#659) は本 Issue では消費せず後続 Issue で扱う。Issue #664: ActiveJobKind::SetupBootstrap (6 番目 additive、#[non_exhaustive] 維持) + DesiredAction::SetupBash (5 番目 marker variant、command 非保持) + should_install_setup_bootstrap (AD22 SSOT pure-fn 決定木、ledger overflow fail-closed)
- `src/agent/skills/*` — SkillRegistry 基盤 (`AgentSkill` trait, `SkillTrustTier`)
- `src/session/*` — セッションストア / `FeedbackFrame` / `Precaution` / `AnvilScore` / `CaseRecord` / `eval_log` / `case_retrieval` / `case_photon_bridge` / `tmp_tests` / `rollout_policy` / `feedback.rs::redact_verifier_command_for_storage` (verifier command redactor SSOT) / `store.rs::VerifierInvocationRecord` (Phase α-2)
- `src/session/job_report.rs` — Issue #666: session-layer JSONL persistence for structured job reports. `pub(crate) struct StructuredJobReportRecord` (`schema_version: u32` envelope-level / `recorded_at_unix_ms: u64` SystemTime + UNIX_EPOCH per DR1-001 / `turn_index` / `event_name` / `envelope: Value`) + `pub(crate) const JOB_REPORT_SCHEMA_VERSION: u32 = 1` + caps (`MAX_JOB_REPORT_LINE_BYTES = 16 KiB`, `MAX_JOB_REPORT_FILE_BYTES = 16 MiB`). `SessionStore::{job_reports_path, append_job_report, read_job_reports}` extension append/read `sessions/<id>/job-reports.jsonl` with path-traversal whitelist (`is_safe_session_id`) + line-cap/file-cap DoS defence (DR4-002/003) + schema_version mismatch skip+WARN (DR1-005). DR3-002 unidirectional dependency: no `crate::agent::*` imports. `pub(crate) mod job_report;` registration; no `pub use` re-export.
- `src/tools/*` — built-in tools (`bash.rs::check_blocked_command` SSOT, Read/Write/Edit, `test_output.rs` failed-name + trim formatter) + Issue #664: bash.rs::is_setup_command (BashCommandClass::EnvSetup thin wrapper SSOT、AD2/AD12/DR1-001 案 B)
- `src/repo_graph/*` — `RepoGraph` v1 (import scan + LRU persist)
- `src/util/file_classify.rs` — `is_test_file` / `is_setup_file` / `is_implementation_file` SSOT
- `src/util/git_hardened.rs` — `run_git` SSOT (hardened git command runner)
- `src/logging.rs` — `log_llm_event` + `mask_payload_inplace` / `is_secret_like_key` + `stable_path_hash` (Issue #661: 16-hex DefaultHasher SSOT、`mask_secrets` 適用後の入力前提) + `compute_payload_digest` (Issue #661: per-turn payload dedup SSOT、`mask_payload_inplace` 適用後の `serde_json::Value` を `DefaultHasher` で `[u8; 8]` digest 化、non-cryptographic correlator)。`stable_path_hash` の caller は `artifact_ledger.rs` / `turn.rs::build_active_job_selected_payload` / `active_job_arbiter.rs` test (`#[cfg(test)]`) / 後続 #661 Phase 5 の `auto_test.rs::PathHashHex`。
- `src/config.rs` — CLI / env / `.anvil/config` のマージ
- `src/model_registry.rs` — 利用可能モデルとメモリ量から main / sidecar を選択
- `src/ollama/*` — Ollama HTTP 連携 + `<think>` strip / XML tool call 回収
- `src/modes/plan_act.rs` — Plan / Act mode 状態 + WorkMode 二段階分類純関数
- `src/tui/markdown.rs` — assistant 応答の SGR markdown renderer
- `src/git/checkpoint.rs` — checkpoint / rollback

## Layer Rules (不変条件)

- **DR3-001**: `pub(crate)` 内部 API は `src/photon/mod.rs` 等の facade から re-export しない（module unit tests のみで検証）
- **DR3-002**: photon 層は session 層 / agent 層を import しない（photon → session → agent の単方向依存）
- **DR3-003**: log filter / OnceLock logger 系は unique session_id で隔離（test isolation）

## Security Invariants

- すべての外部由来文字列は `session::feedback::mask_secrets` を通す
- 永続化 / log payload は `logging::mask_payload_inplace` を **最終防衛線** として通す
- summary_id 系の正規化は `src/photon/prompt.rs::sanitize_summary_id` を **SSOT** として注入側 / 評価側の双方で必ず通す (DR4-002)
- localhost / credential なし URL 制約: `validate_localhost_url`

## Development Expectations

- 旧アーキテクチャへ戻す方向の継ぎ足しはしない
- provider abstraction を増やさない
- local LLM 向けの prompt / loop の単純さを優先する
- 新機能は E2E 寄りの検証を伴わせる（mockito ベース、Ollama 不要が望ましい）
- per-turn cap が必要な hook は `<feature>_called_this_turn: bool` を `Agent` / `SessionSnapshot` に追加し、`handle_user_message` 冒頭で reset

## Issue 番号と詳細仕様の所在

- 完了済 Issue の実装詳細 → `git log` / `gh issue view <N>` / `gh pr view <N>`
- 各 Issue の設計判断 → `dev-reports/design/issue-<N>-*.md` / `dev-reports/issue/<N>/`
- アーキテクチャ全体 → コード（モジュール単位で `mod.rs` doc comment 参照）

## Release Process

変更しない。

- package / binary は `anvil`
- `.github/workflows/ci.yml` は `fmt` `clippy` `test` `build`
- `.github/workflows/release.yml` は tag push 時に gzip artifact を作って GitHub Release を切る
